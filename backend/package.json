{
  "name": "mustergo-backend",
  "private": true,
  "version": "1.0.0",
  "description": "MusterGo backend — SCCCI delegation attendance (VJMDynamics). Express API with delegate CRUD + Excel export.",
  "type": "module",
  "scripts": {
    "dev": "node --watch server.js",
    "start": "node server.js"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "exceljs": "^4.4.0",
    "express": "^4.19.2"
  }
}
