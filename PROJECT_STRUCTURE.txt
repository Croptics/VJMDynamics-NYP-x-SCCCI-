MUSTERGO — PROJECT STRUCTURE NOTES
====================================
(What each file does, organized by folder)


ROOT LEVEL
----------
permissions.js
    The single source of truth for permissions. Both frontend and backend
    import this one file. Add one entry here and the checkbox/chip/
    enforcement appear everywhere automatically. Two kinds now (2026-07-20):
    (1) 5 ACTION permissions (manageDelegates, manageTrips, manageExceptions,
    exportData, manageAccounts) — enforced by the backend via
    requirePermission(), same as always. (2) VIEW permissions
    (viewDashboard/viewDelegates/viewTrips/viewDocuments/viewCheckin/
    viewScanner/viewExceptions/viewHistory for desktop routes, viewMobileHome/
    viewMobileAttendance/viewMobileTrips for /mobile/* routes — viewCheckin
    was briefly removed alongside the /checkin page 2026-07-20, then both
    restored the same day at the user's request: "have a backup of the
    checkin page and put a permission to hide the page") — FRONTEND-ONLY
    gates on whether an account can even see a
    page/tab; no backend endpoint maps 1:1 to "can view a page", so these
    aren't wrapped in requirePermission() — the real data-mutating actions
    on each page stay gated by the existing action permissions exactly as
    before. Each entry has a `group` field ("action"|"desktopView"|
    "mobileView") purely to organize the Account control modal's checkboxes
    into labelled sections (see AccountControlPage.jsx below) — carries no
    other meaning. cleanPermissions() falls back to each permission's own
    `default` (not a hardcoded false) for any key ABSENT from an account's
    stored permissions JSON — this is WHY adding 11 new keys didn't lock out
    every account created before this feature existed: action permissions
    keep their old defaults (mostly closed), but the 11 view permissions
    default true, so a pre-existing account keeps seeing everything it
    already could, and an admin now explicitly UNCHECKS a box to restrict a
    specific page going forward rather than the rollout silently
    restricting everyone. Verified live against the real shared Neon DB:
    a real pre-existing staff account (desmond, created weeks before this
    change) now returns all 11 view keys as true with zero DB writes.

README.md / HIGH_LEVEL_DESIGN.md / PROJECT_IMPLEMENTATION_PHASE.md
    Docs — setup instructions, architecture/DB schema notes, and the team's
    sprint plan.

.gitignore
    Excludes .claude/, node_modules/, and other local-only files from git.

.claude/
    My (Claude Code) own tooling config — dev-server launch settings etc.
    Not part of the app itself; gitignored, never pushed to GitHub.

qr-test-codes/
    Two ready-made QR images for testing the /checkin screen's QR pill
    (qr-attendance-registered.png = a valid MusterGo delegate badge,
    qr-code-invalid.png = junk that should show "QR code invalid") plus
    qr-payloads.txt with the raw JSON each one encodes.


BACKEND/  (Express API, port 4000)
-----------------------------------
server.js
    The Express app itself. Defines every API route: login/session,
    dashboard reads, delegate CRUD, Excel export, account management.
    Read routes (trips/dashboard/missing/delegates) require any signed-in
    account via requireAuth(); mutating routes and export require a
    specific permission via requirePermission() (both from auth.js). Has a
    "TEAMMATE ZONE" comment near the bottom — the sanctioned spot for
    teammates to plug in their own route files (e.g. routes/insights.js
    is mounted here).
    The credential endpoints (POST /api/auth/login and /api/auth/reset-
    password) are wrapped in a tiny in-memory rateLimit() middleware —
    10 attempts per 10 minutes per IP, returns 429 + Retry-After when
    exceeded, and a successful login clears that IP's counter so normal use
    is unaffected. No dependency and no shared store (fine for this single-
    instance app; a multi-instance deploy would move it to Redis / express-
    rate-limit). Also warns at startup if FRONTEND_URL isn't set, because
    CORS then reflects ANY origin — fine for the local Vite proxy, must be
    set before a public deploy (mirrors auth.js's JWT_SECRET warning).

    POST /api/auth/logout — requireAuth()-gated; calls clearLastSeen() on the
    caller's own account so Staff Operations' "active now" list reflects an
    explicit logout right away. Frontend fires this best-effort (fire-and-
    forget, errors ignored) from App.jsx's handleLogout BEFORE clearToken()
    wipes the JWT it needs — the local logout (clearToken + redirect to
    /login) proceeds regardless of whether this call succeeds.

    POST /api/auth/kiosk (2026-07-21) — PUBLIC, no auth at all, on purpose.
    Mints a scoped, passwordless "kiosk" token (auth.js's signKioskToken(),
    a JWT carrying {kiosk:true} and NO username) for the entrance-kiosk
    scanner. The token grants NOTHING by default — accountFromReq() (which
    every requireAuth()/requirePermission() route uses) resolves a
    username-less token to null and rejects it — it only works on the two
    routes that explicitly opt into the new requireKioskOrAuth() middleware
    (auth.js): POST /api/attendance/scan (vimal.js) and POST /api/onboarding
    /checkin (vance.js). requireKioskOrAuth() accepts a real session OR a
    valid kiosk token; for a kiosk token it maps the request to a dedicated
    `__kiosk__` backing account (seeded once in data.js's seed(), hidden
    from listAccounts()/Account control/staff counts by username) so writes
    that foreign-key onto accounts.id (check_in_logs.checked_in_by) still
    resolve. See KioskScannerPage.jsx below for the frontend half. Verified
    via curl: a fresh kiosk token gets 401 on /api/accounts and
    /api/trips/t-1/dashboard, but passes auth on /api/onboarding/checkin
    (a 404 UNKNOWN_CODE for a bad badge — a data error, not an auth error);
    a real end-to-end kiosk QR check-in flipped a real delegate MISSING→
    PRESENT with zero credentials, then reverted.

auth.js
    Session tokens + auth middleware, shared by server.js and any
    teammate route file that needs to require a signed-in caller (e.g.
    routes/insights.js, routes/desmond.js, routes/exceptions.js). Issues
    signed JWTs (makeToken) and exports accountFromReq / requireAuth /
    requirePermission. accountFromReq also accepts the token as a ?token=
    query param (not just the Authorization header) — needed for Jayden's
    SSE route, since EventSource can't set custom headers. Set JWT_SECRET in
    backend/.env before deploying anywhere public — without it a fixed
    dev-only fallback is used and a warning prints on startup.

data.js
    The database layer — talks to PostgreSQL. Creates the schema/seed
    data on first run, and exports functions like getDashboard(),
    getMissing(), listDelegates(), account CRUD, etc. server.js just
    calls these. Passwords are stored as bcrypt hashes (hashPassword /
    verifyPassword). Older rows created before hashing was added are
    auto-upgraded in place the next time that account logs in
    successfully (upgradePasswordIfNeeded) — no manual migration needed.
    Also creates Desmond's "TransitFlow" schema (the users + itinerary_items
    tables and the extra trip/coach/delegate columns his Trips board needs) —
    folded into createSchema()/seed() so it auto-applies on startup, instead
    of the standalone SQL migration files his feature originally shipped with.
    delegates."photoUrl"/"photoPublicId" (profile photos, see cloudinary.js
    below) are set ONLY via setDelegatePhoto()/clearDelegatePhoto() — never
    through the generic createDelegate/updateDelegate path — so a client
    can't PATCH in an arbitrary external image URL and skip the upload
    validation.
    delegates."lastLocation" — a free-text place/address, staff-entered via
    the normal create/edit path (normalize()/createDelegate/updateDelegate),
    unlike photoUrl. Not geocoded server-side; the frontend's
    DelegateLocationMap component hands the raw text straight to the Google
    Maps Embed API's `q=` param, which does its own lookup. getMissing() also
    includes it in its output alongside lastSeen. getMissing() also now
    returns each delegate's coachId (needed so DashboardPage.jsx's Reverse
    headcount tab can group missing delegates by coach; it previously only
    returned a pre-formatted "Coach 7 · Suzhou" display string with no id).
    getMissing() also now carries each row's status (always "MISSING" there,
    but DelegateAvatar reads it to tint the avatar red).
    activity_log.actor — the account name that performed each change, so the
    History tracker / History log show WHO did it instead of a generic "you".
    logActivity(text, kind, actor) records it; createDelegate/updateDelegate/
    deleteDelegate/deleteAllDelegates take an optional actor param that
    server.js's delegate routes fill from req.account (actorOf() =
    name || username). rowToActivity() returns it as `via`, falling back to
    "you" for legacy rows / writes with no actor (e.g. teammate modules like
    vimal.js/vance.js that call createDelegate/updateDelegate directly for
    check-ins don't pass one — those show "you"). Nullable column, added via
    ALTER ... ADD COLUMN IF NOT EXISTS, so existing rows aren't attributed
    retroactively (they keep showing "you").
    delegates.createdBy — same actor-snapshot pattern as activity_log.actor,
    but persisted ON the delegate row itself (not just the audit log) so it
    can be its own Dashboard table column ("Created by"). createDelegate()
    already took an `actor` param for the activity-log entry; this just also
    writes it into the new column and returns it on the created row. Wired
    from server.js's POST /api/trips/:id/delegates (single add) and
    vance.js's POST /api/trips/:id/onboarding/confirm (bulk upload) — both
    already had req.account available via their permission gate. NULL/"—"
    for legacy rows or any other creation path that doesn't pass an actor.
    5-status system, step 1 (Dashboard-only basic CRUD) — normalize() (in this
    file) expanded its allowlist from 3 values (PRESENT/MISSING/UNASSIGNED) to
    5 (UNASSIGNED/ASSIGNED/ARRIVED/LATE/MISSING). "PRESENT" is kept as a
    legacy alias mapped to ARRIVED inside normalize() itself — any row that
    still says PRESENT (from before the 2026-07-18 QR/face-scan → ARRIVED
    migration below, or any future writer that reintroduces the legacy
    literal) keeps working instead of silently resetting to UNASSIGNED. Only
    DashboardPage.jsx's status <select>s (filter dropdown +
    the add/edit modal) were updated to offer all 5 values; every other
    consumer (TripCoachPage, mobile attendance, QR/check-in screens) still
    only knows the original 3 and is unaffected either way — this is
    deliberately narrow, not a full rollout (see AI Log). StatusBadge.jsx's
    STATE_MAP gained ASSIGNED/LATE entries (new badge-assigned/badge-late CSS
    classes + --st-assigned/--st-late tokens in tokens.css, light+dark) —
    additive only, doesn't change PRESENT/MISSING/UNASSIGNED rendering.
    DashboardPage.jsx also gained a local effectiveStatus(d) helper (aliases
    PRESENT→ARRIVED) used only for the status filter/sort, so legacy rows
    still show up correctly under the "Arrived" filter instead of vanishing.
    Step 2 (done): TripCoachPage.jsx (Desmond's Trip page) — same additive
    pattern. Its own local STATUS_LABEL/STATUS_COLOR2 maps (a THIRD copy of
    the enum, independent of data.js's normalize() and StatusBadge's
    STATE_MAP — see the "5 duplicate enum maps" note in the AI Log) gained
    ASSIGNED ("blue")/LATE ("orange") entries, PRESENT aliased to the
    "Arrived" label/green color same as everywhere else. UNASSIGNED was
    "yellow" originally; changed to "grey" (2026-07-19, uses the existing
    --tf-grey token) so the delegate-details panel's status chip matches the
    muted-grey-for-Unassigned convention adopted app-wide the same day (see
    DelegateAvatar.jsx's statusTone()) — distinct from the yellow still used
    for the unrelated "unassigned drop-zone" empty-coach-lane fleet card.
    TripCoachPage.css
    gained matching --tf-orange/--tf-orange-bg/--tf-orange-line tokens
    (light+dark) — this page has its OWN --tf-* color variables, entirely
    separate from tokens.css's --st-* ones, so both had to be extended
    independently. The status filter <select> expanded from 3 to 5 options,
    with the same PRESENT→ARRIVED filter-comparison alias DashboardPage.jsx
    uses. This page still has no direct status editor (unlike Dashboard's
    dropdown) — status only changes via drag-to-coach or the "Move to coach"
    control (handleReassign()). Step 3 (done, follow-up pass): that
    transition originally only knew UNASSIGNED/MISSING (delegate.status ===
    "UNASSIGNED" ? "MISSING" : delegate.status when dropped on a coach) — so
    dropping someone onto a coach immediately marked them MISSING, which
    conflated "just assigned, hasn't had a chance to board yet" with
    "genuinely failed to show up." Changed the UNASSIGNED branch to ASSIGNED
    instead: dropping onto a coach now means "expected here, not yet
    checked in," matching the 5-status model's actual intent — MISSING is
    reserved for a real check-in miss or an explicit staff override, not the
    default outcome of a routine assignment. A delegate who already has a
    real status (ARRIVED/LATE/MISSING/ASSIGNED) still keeps it when moved
    between coaches — only the UNASSIGNED→? branch changed, moving someone
    who already arrived doesn't reset them. Deliberately NOT touched:
    FleetCard's per-coach "X missing" badge, the Hero banner's missingCount
    alert, and DelegateCard's red "is-missing" tint all still check
    `status === "MISSING"` literally, unchanged. This is intentional, not an
    oversight — those counts now more accurately mean "genuinely missing"
    instead of "assigned but not literally PRESENT" (the old 3-status
    reading), which was itself the design flaw being fixed. The visible
    side effect: a delegate freshly assigned via drag no longer inflates any
    "X missing" count anywhere until something actually marks them MISSING
    (today: only a manual override; the Late/10am-cutoff auto-transition is
    still unbuilt — see the deferred items in the 5-status plan). Dashboard's
    and Mobile's own "missing" counts/queries are NOT touched in this pass
    either and will have the same read going forward — a coherent, if
    still-incomplete, picture until the Late/cutoff system exists to
    actually move someone from Assigned into Missing over time.
    resolveTripUuid("t-1") now returns the REAL Beijing trip uuid instead of
    null. WHY (fixed two linked bugs): "t-1" used to mean null = "every
    delegate/coach across ALL trips". Fine while only Beijing had data, but
    once other trips got their own coaches/delegates it meant (a) the base
    Dashboard's Coach status listed OTHER trips' coaches (looked like stray
    placeholders — e.g. 8 coaches shown when Beijing has 7) and (b) a delegate
    added on the base Dashboard was created with trip_id = NULL, so it showed
    on the Dashboard but NEVER on Desmond's Trips board (which filters by trip
    uuid) — e.g. "desmond" was missing from Trips. Scoping "t-1" to Beijing
    makes the base Dashboard and the Trips board the exact same dataset
    (verified: both 34 delegates / 7 coaches, desmond on both). The existing
    seedTransitFlow() startup backfill still attaches legacy NULL-trip rows to
    Beijing so nothing vanishes. Every base-Dashboard read/write (getDashboard,
    getMissing, listDelegates, createDelegate, deleteAllDelegates, export) goes
    through resolveTripUuid, so this single change scopes them all — no
    per-route edits. Note the semantic shift: "Delete all" and "Add delegate"
    on the base Dashboard now act on the Beijing trip specifically, not
    literally every trip's delegates.
    accounts.last_seen_at — stamped by touchLastSeen() every time GET
    /api/auth/session is called (every signed-in client already polls that
    every 15s for the force-logout guard), so this adds no new client
    traffic. listActiveAccounts(withinSeconds=45) returns accounts seen
    within that window — powers the Dashboard's "Staff operations" tab
    "active now" list. clearLastSeen(id) sets it back to NULL — called by
    POST /api/auth/logout so an explicit logout drops off the active list
    immediately instead of waiting up to 45s to age out.

cloudinary.js
    Thin wrapper around the Cloudinary SDK for delegate profile photos.
    uploadImage() forces resource_type:"image", so Cloudinary itself
    decodes/validates the bytes are a real image — not just trusting the
    client's declared Content-Type. Needs CLOUDINARY_CLOUD_NAME/API_KEY/
    API_SECRET in backend/.env (sign up at cloudinary.com — free tier is
    plenty); isConfigured() lets server.js return a clear "not configured"
    503 instead of failing oddly when those aren't set.
    Media manager additions (2026-07-19): listImages()/destroyImages()/
    purgeAllImages() use the Cloudinary Admin API (cloudinary.api.*, distinct
    from the Upload API — cloudinary.uploader.* — the per-delegate flow
    above uses). All three are hardcoded to DELEGATE_PHOTO_FOLDER
    ("mustergo/delegates") — the ONLY folder any uploadImage() call in the
    codebase ever writes to — so "purge all" can never reach outside what
    this app itself put there, even on a shared Cloudinary account. Backs
    routes/media.js's admin-only bulk image management UI (Settings → Image
    storage). destroyImages() chunks at 100 publicIds per call (Cloudinary's
    own cap on delete_resources); purgeAllImages() lists then bulk-deletes
    the exact ids just listed (rather than delete_resources_by_prefix) so
    the caller gets back precisely what was removed, to reconcile against
    the delegates table.

routes/desmond.js
    Desmond's "TransitFlow" API — the Trips & Coaches board (list trips,
    per-trip coach/delegate/itinerary CRUD, staff directory, seed demo
    trips, in-memory activity feed). Self-contained: makes its own pg pool.
    "View for all, edit gated": every GET route (and the cosmetic POST
    .../activity) uses requireAuth() (any signed-in account); every other
    write route uses requirePermission("manageTrips") — applied per-route,
    not as one router-level gate, since a blanket gate would also catch
    requests meant for OTHER teammates' routers mounted alongside this one
    (found and fixed while merging in Jayden's exceptions.js). Mounted in
    server.js's TEAMMATE ZONE next to insightsRouter. To avoid colliding
    with the base app's hardcoded single-trip routes it uses new paths like
    /api/all-trips and /api/trips/:tripId/summary.

routes/exceptions.js
    Jayden's Exception Logging & QR Fallback API — ticket CRUD (raise/
    resolve/delete), manual attendance override, and a live Server-Sent
    Events channel (GET /api/exceptions/stream) that pushes ticket changes
    to every connected staff device in real time. Self-contained: makes its
    own pg pool and creates its own tables (exception_tickets, shared
    check_in_logs) via initExceptions(), called once from server.js's
    startup chain after initDb(). "View for all, edit gated": reads (+ the
    SSE stream) use requireAuth(); raising/resolving/deleting a ticket or a
    manual override use requirePermission("manageExceptions"). Its original
    auth code decoded the old pre-JWT token format by hand — rewritten at
    merge time to use auth.js's requireAuth/requirePermission like every
    other route. Also has POST /api/checkins/qr (Jayden's QR half of the
    /checkin screen — writes a method='QR' row to the shared check_in_logs
    table and flips the delegate ARRIVED) — gated on requireAuth() only,
    same level as Vimal's face scan, not the elevated manageExceptions used
    by manual overrides. Idempotent on client_event_id. NOTE: this route has
    no frontend caller anywhere (ScanToBoardView.jsx, the only page that
    would've used it, was itself unrouted/dead and has since been DELETED
    2026-07-19) — kept fixed for consistency with the other check-in routes,
    but confirmed orphaned as of 2026-07-18.
    QR/face-scan → ARRIVED migration (2026-07-18): both this route and
    POST /api/checkins/manual (the LIVE endpoint ManualTrackingPanel.jsx
    calls) used to write the legacy 'PRESENT' literal via raw SQL, bypassing
    data.js's updateDelegate()/normalize() aliasing entirely — now write
    'ARRIVED' directly. /api/checkins/manual's duplicate-check response and
    /api/checkins/qr's duplicate-check + success responses were updated to
    match. See vance.js and data.js entries below for the other two routes
    in this same migration.
    exception_type has a 6th value, "OTHER", carrying a free-text label in
    the new exception_tickets.type_other column (VARCHAR(TYPE_OTHER_MAX=20),
    added via ALTER TABLE ... IF NOT EXISTS so an already-deployed DB picks
    it up on next boot). exception_priority already had "LOW" in its enum
    from the start, but nothing in the UI ever exposed it until this same
    change — Critical/Normal/Low is now a real 3-tier system end to end.
    POST /api/trips/:id/exceptions validates type against the enum itself
    (400 instead of an opaque Postgres 500 on a bad value), requires
    typeOther (trimmed, capped) when type is "OTHER", and validates priority
    the same way. Cherry-picked from Jayden's upstream branch
    (`origin/SecureScan-Logs-(Jayden)`) — see the "INTEGRATING TEAMMATE
    BRANCHES" note near the bottom of this file for why this was a
    selective port, not a file merge.

routes/vimal.js
    Vimal's FaceCheck-Pro — Privacy-First Biometric & Multi-Modal Fusion
    Scanner. Powers the /checkin screen's "Trips" (reverse-headcount muster
    view), "Scan → Face" tab, and "Me" (consent + history) tab. Self-
    contained: imports JQ's own listDelegates/getDelegateById/updateDelegate/
    createDelegate/getTrip/getDashboard from data.js (never edits it), so a
    face check-in writes through to the REAL shared delegates table and
    every other screen (dashboard, mobile, Trips board) sees it instantly.
    Coach lookups go through getDashboard(), so coaches added later by
    Desmond's module work here automatically. PDPA-by-design: the server
    NEVER receives or stores an image/audio clip — the browser vectorizes
    one off-DOM camera frame into an irreversible token ("face:v1:<hex>:…"),
    zeroes the raw pixel buffer in place, and only the token is ever sent.
    Consent lifecycle + per-delegate check-in history are kept in-memory
    (Maps, not the DB) — Vimal-owned bookkeeping, separate from the shared
    delegate records. Endpoints: GET /api/attendance/coaches, POST /api/
    attendance/scan, GET /api/attendance/:trip_id/coach/:coach_id, GET /api/
    attendance/headcount, POST /api/attendance/consent, GET /api/attendance/
    history/:delegate_id, POST /api/attendance/assign-unassigned (muster
    UNASSIGNED delegates onto a coach), POST /api/attendance/demo-seed. All
    gated on requireAuth() only — any signed-in staff can scan, matching the
    QR check-in's auth level rather than the elevated manageExceptions tier.
    Shares the /checkin screen with Jayden's QR/Manual/Issues panels (see
    QRCheckInPage.jsx below) — the two features were built independently
    against reserved slots in the same shell and verified not to collide
    (own camera streams, no shared re-fetching, namespaced CSS/routes).

    Boarded-count sync bug fix (2026-07-20): GET /api/attendance/:trip_id/
    coach/:coach_id and GET /api/attendance/headcount both computed
    `boarded: payload.filter(d => d.status === "PRESENT").length` — checking
    ONLY the legacy PRESENT value, never the current ARRIVED value that
    manual overrides/QR scans/face scans actually write. GET /api/
    attendance/coaches (the coach-LIST endpoint, powers the /scanner and
    /checkin coach dropdown) goes through data.js's getDashboard(), which
    already has the correct `isBoarded = (x) => x.status === "PRESENT" ||
    x.status === "ARRIVED"` — so the two endpoints had quietly drifted out
    of sync since the ARRIVED migration, and the coach-DETAIL endpoint's own
    BOARDED/MISSING side-panel tiles undercounted every ARRIVED delegate
    while the dropdown showed the right number for the same coach. Fixed
    both occurrences to match isBoarded's OR-check. Verified live: /scanner's
    Coach 7 dropdown and BOARDED tile both read 6, matching (were 6 vs 1
    before the fix in the reporting screenshot).

routes/vance.js
    Vance's DocuSync AI + Trip Assistant API — three features. (1) Document
    parsing: an uploaded directory / attendee list / spreadsheet / scanned
    passport is read (text via the `unpdf` package, or Claude vision for
    scans/images) and turned into structured delegate rows with a confidence
    score; on confirm they're written to the SHARED delegates table via
    data.js's createDelegate(), and each gets a unique `qr_code` (e.g.
    "MG-86B620A4") minted by newQrCode(). (2) QR boarding passes + on-site
    check-in: GET /api/onboarding/badges returns every delegate + their QR
    token for a trip (auto-backfills a qr_code for any delegate that predates
    this feature, via backfillQrCodes()); POST /api/onboarding/checkin
    resolves a scanned/tapped code, flips the delegate ARRIVED (+ coach —
    `alreadyBoarded` fixed 2026-07-20 to also check status==="ARRIVED", not
    just the legacy "PRESENT", same bug class as the vimal.js fix above —
    otherwise a QR re-scan of someone already boarded via face/manual
    incorrectly reported alreadyBoarded:false instead of the duplicate
    notice), and
    logs a QR row to the SHARED check_in_logs table — the same table Jayden's
    manual override and Vimal's face scan write to, and the one Desmond's
    coach board reads, so a boarding-pass scan shows up everywhere instantly.
    The logged trip is derived from the DELEGATE's own trip (JOIN through
    trips.uuid_id), not the client-supplied tripId — so a mistyped/mismatched
    tripId in the request can't file a check-in against the wrong trip; it
    only falls back to the request's tripId for base-pool delegates with no
    trip set.
    Both gated on requireAuth() only (any signed-in staff), matching Vimal's
    face-scan auth level rather than the elevated manageExceptions tier.
    QR/face-scan → ARRIVED migration (2026-07-18): this is the LIVE endpoint
    QRScannerPanel.jsx actually calls (via qrCheckin()). Previously wrote the
    legacy 'PRESENT' literal via raw SQL and computed an `alreadyBoarded`
    re-scan guard that was never actually enforced (the write happened
    regardless) — now writes 'ARRIVED', the guard is enforced (a re-scan
    returns `alreadyBoarded:true` without re-writing/re-logging), and the
    trip's headcount SQL recognizes both the legacy and current value.
    (3) Trip assistant chatbot: answers questions over a live snapshot of
    everyone's data (delegates/coaches/exceptions/check-ins/itinerary),
    streaming replies token-by-token, with saved chat history. The prompt is
    strict about not mixing up present/missing/unassigned and about not
    volunteering unrequested detail (answer only what was asked, then stop).
    Self-contained: own pg pool, own tables created lazily on first use via
    ensureReady() (extra delegate columns incl. qr_code + a partial unique
    index + chat_sessions/chat_messages). Parsing and confirming require
    manageDelegates; the chatbot and the boarding-pass/scan endpoints are any
    signed-in user. Parsing prefers Claude (needs ANTHROPIC_API_KEY; required
    for scans); the chatbot is Ollama-first then Claude, like insights.js.
    GET /api/onboarding/context, POST /api/trips/:id/onboarding/confirm, and
    GET /api/onboarding/badges now resolve their tripId via data.js's shared
    resolveTripUuid() instead of an ad-hoc `SELECT uuid_id FROM trips WHERE
    id = $1`. WHY: trips.id is a legacy short code that's ONLY ever "t-1" for
    the seed trip — every other trip's `id` column holds an unrelated random
    uuid (see desmond.js's `gen_random_uuid()::text` in its trip-seed
    INSERT) — so the old lookup silently matched nothing for any trip except
    the base one, and every write/read fell back to unscoped (trip_id NULL)
    behavior. This was the real cause behind "pre-event uploads don't reach
    the arrival check-in module" for any non-Beijing trip. onboarding/confirm
    also now passes the resolved tripUuid directly into createDelegate()'s
    2nd param (not just a follow-up UPDATE), so a delegate is correctly
    scoped from its very first INSERT. POST /api/onboarding/checkin's own
    trip-derivation (LEFT JOIN through the delegate's own trip_id, see the
    comment there) was already correct and untouched.

    UPDATE MERGED FROM VANCE'S BRANCH (2026-07-20 — see INTEGRATION_NOTES.md
    at the project root, "Feature 3 — DocuSync AI + Trip Assistant" section,
    and AI Log entry for full detail; the standalone INTEGRATION_NOTES_VANCE.md
    was folded into that doc on 2026-07-21): pulled
    in Vance's newer v2DocuSync-AI branch, ~525 new lines here plus updates
    to claudeParse.js/OnboardingPage.jsx/BoardingPassesView.jsx/
    ChatAssistantPage.jsx and a brand-new TripPulse.jsx header widget.
    New: passport-expiry validation (checkPassportExpiry — flags expired/
    expiring-within-6-months passports on the onboarding review cards, in
    the assistant, and in risk scoring); a deterministic fast-path
    (answerLocally) that answers common factual questions instantly from
    the snapshot with no model call (works even with no AI engine reachable);
    risk scoring (computeRisk) that ranks "what to watch" for the assistant's
    prompt and TripPulse's header widget; local Tesseract OCR fallback for
    scanned documents when no ANTHROPIC_API_KEY is set (new tesseract.js
    dependency); snapshot caching + a chat-model warm-up call for speed;
    chat sessions can now be pinned (chat_sessions.pinned column, additive).
    New dependency: tesseract.js (installed, backend/package.json +
    package-lock.json updated; also added *.traineddata to backend/
    .gitignore since Tesseract downloads its language data at runtime).
    ChatAssistantPage.jsx was HAND-MERGED, not overwritten — Vance's branch
    predates the `embedded` prop this file gained for the floating
    ChatBubble widget (see ChatBubble.jsx), so a blind copy would have
    broken the bubble (full page chrome + duplicate header inside the small
    floating panel). Kept `embedded` and all its conditionals intact, and
    layered in only Vance's real additions: the TripPulse header widget
    (shown only in the non-embedded/full-page view), the auto-growing
    multi-line composer (Shift+Enter for a newline), the nicer empty-state,
    and disabling Send while the draft is blank. Also deliberately did NOT
    adopt Vance's `var(--ink-solid)` → `var(--ink)` token change on the
    avatar backgrounds — `--ink-solid` stays dark in both themes (needed for
    the white Bot icon on top of it), while `--ink` flips to near-white in
    dark mode, which would have made the icon invisible (the exact bug
    fixed on the /scanner Refresh button the same day — see that AI Log
    entry). mobile/MobileAssistantPage.jsx and the `html5-qrcode` frontend
    dependency were deliberately SKIPPED: MobileAssistantPage's only diff
    on Vance's side was that same stale ink-solid→ink change (main's copy
    is already ahead, unrelated to this feature set), and html5-qrcode was
    intentionally uninstalled earlier this session as dead code (see the
    dead-code-audit AI Log entry) — pulling it back in would undo that.
    Verified live end-to-end after merging: Onboarding page's Document
    parsing tab (TripPulse widget rendering real onboarding-progress data)
    and Boarding passes tab both load with zero console errors; the
    floating ChatBubble opens correctly with the new auto-growing textarea,
    no TripPulse/history-sidebar leaking into the embedded panel, header
    text appears exactly once (no duplicate-header regression); sent a real
    message through the new composer and got a real instant fast-path reply
    from GET /api/assistant/pulse's risk scoring. Backend restarted cleanly
    under `node --watch` with no startup errors after the vance.js swap.

backend/seed-demo.js
    One-off script (npm run seed:demo) that seeds a realistic delegate
    roster (via data.js's own createDelegate, so rows are shaped exactly
    like the rest of the app expects) plus 7 sample exception tickets, so
    the Exceptions inbox and Trips board aren't empty on a fresh database.
    Safe to re-run — skips seeding whatever already exists.

backend/seed-team.js
    One-command setup (npm run seed:team) that creates ONE login account per
    team member (jq / vance / desmond / jayden / vimal, each "password123!",
    full access), so nobody has to share staff_194. WHY IT MATTERS: logins
    enforce a single active session (auth.js bumps token_version each login),
    so when the whole team shares staff_194 they constantly log each other
    out ("Your account is no longer valid" every ~15s). Using your OWN account
    fixes it. Safe to re-run — never overwrites an account that already exists.

backend/reset-login.js
    One-command fix (npm run reset:login) for "I cloned the repo and can't
    log in." The team shares ONE Neon database and `git clone` only resets
    CODE, not the DB — the seeded staff_194/password123! login only applies
    on a first boot against an empty DB, so once the shared DB drifts (a
    changed password, a teammate's old branch writing to accounts) the
    documented credentials stop working. This force-resets staff_194 back to
    password123! with full main permissions (creating it if missing). Safe
    to re-run; override target with RESET_USER / RESET_PASS env vars.

routes/insights.js
    Powers the "Generate Insights" button on the Dashboard's Analytics tab.
    Requires a signed-in account (requireAuth, from auth.js). Tries a
    local Ollama server first (free, no key, nothing leaves your
    machine); if Ollama isn't running, falls back to the Anthropic API
    (needs ANTHROPIC_API_KEY in .env). If neither is available, returns a
    clear "not configured" message instead of erroring. Also honors the
    current UI language — asks the model to write directly in English or
    Chinese, so no separate translation step is needed. buildPrompt() now
    forces a fixed 3-point structure — "1. Overall: ... / 2. Missing: ... /
    3. Advice: ..." (translated labels in SECTION_LABELS for zh) — instead
    of freeform 3-5 points, so staff always get the same read: attendance
    health, who's missing, then concrete next actions/who to contact. Found
    via live testing (local Ollama): the instruction telling the model the
    label-prefix pattern used a literal meta-placeholder — `"N. ${"{Label}"}:
    "` — which a weak local model echoed VERBATIM into every point ("N.
    Overall: ...") instead of substituting a real digit. Fixed by spelling
    out the substitution rule in plain English with a concrete worked example
    ("the first point must literally begin '1. Overall: '") and an explicit
    "never write the letter N" instruction.

routes/export.js
    Customizable multi-sheet Excel export (added 2026-07-19 — replaced the
    old single-flat-sheet GET /api/trips/:id/export that used to live inline
    in server.js). Builds an ExcelJS workbook with TWO sheets modelled on the
    reference "Headcount Planning" template: a "Summary" dashboard sheet
    (trip header + generated/filters line, a KEY METRICS band = Total/Arrived/
    Missing/Late/Assigned + an Unassigned line, a per-coach breakdown table
    with % boarded, and an OPTIONAL AI summary note block) and a "Delegates"
    detail sheet (only the selected columns, only the filtered rows, status-
    tinted, sorted Missing-first, frozen header). Endpoints:
    · POST /api/trips/:id/export   — filtered/configurable (exportData perm).
      Body: { statuses[], coachIds[] (incl. "__unassigned"), vipOnly, columns[],
      includeAiSummary, lang }. Empty array in a dimension = no constraint
      there. `lang` ("en"|"zh") is an EXPLICIT, INDEPENDENT per-export choice
      — set via ExportModal.jsx's own "Workbook language" selector, not
      implicitly tied to the requesting staff member's own UI language (they
      might work in English but need to hand a Chinese report to someone
      else). Every sheet name/header/status label/generated message in the
      workbook is translated via a small self-contained `T = {en, zh}`
      dictionary local to this file (can't reuse frontend/src/lib/i18n.jsx —
      different runtime); the filename itself stays plain-ASCII regardless.
    · GET  /api/trips/:id/export   — unfiltered, back-compat (exportData perm).
    · GET  /api/trips/:id/export/options — columns/statuses/coaches metadata
      the ExportModal populates its controls from (requireAuth).
    · POST /api/trips/:id/export/ai-filter — natural-language → the SAME
      { statuses, coachIds, vipOnly } shape the checkboxes produce (exportData).
      BOUNDED + TRANSPARENT by design: mirrors insights.js's provider strategy
      (local Ollama first, Anthropic fallback, graceful 503 "not configured"),
      the model output is sanitized against the real coach ids + status vocab,
      and the UI shows the result as editable checkboxes — the model never sees
      or writes delegate data, only translates the request into a filter. All
      status comparisons normalize PRESENT→ARRIVED so counts/filters are
      consistent regardless of which check-in route wrote the row.
    Ollama JSON-mode bug (fixed 2026-07-19): the shared tryOllama() helper
    hardcoded `format: "json"` on every call — correct for ai-filter (which
    must return parseable JSON) but wrong for maybeAiSummary()'s freeform
    prose "AI summary sheet note", which reused the same helper. Forced into
    JSON mode with no schema to fill, Ollama just returned an empty `{}`,
    which is what showed up in the Summary sheet instead of an actual
    written recap. tryOllama() now takes an opt-in `{json:true}` flag,
    defaulting to prose mode — only the ai-filter call site sets it.
    AI summary format (2026-07-19): rewritten to the SAME fixed numbered
    structure as the Dashboard's "Generate Insights" panel (insights.js) —
    1. Overall / 2. Missing / 3. Advice — via buildSummaryPrompt() +
    SECTION_LABELS (including the same zh labels and the same "must
    literally begin '1. Overall: '" instruction insights.js needed to stop a
    weak local model echoing a literal placeholder). Was a loose 2-3
    sentence paragraph before. The Summary sheet's AI-note row height
    calculation now also accounts for line count (the text is `\n`-joined
    across the 3 points), not just character count.

routes/media.js
    Image storage management (added 2026-07-19) — Settings → "Image storage"
    admin UI. manageAccounts-gated (same tier as Account control — this is
    an account-wide infra action, not routine delegate editing).
    · GET  /api/media/images — lists every asset in cloudinary.js's
      DELEGATE_PHOTO_FOLDER, cross-referenced against listDelegatesWithPhoto()
      (data.js) so each thumbnail is tagged with which delegate owns it, or
      null/"Unlinked" if orphaned (left over from a since-replaced photo).
    · POST /api/media/images/delete — bulk-delete a chosen publicId[]
      selection; also calls clearPhotoByPublicId() (data.js) to unlink any
      delegate whose photo just got destroyed, so the app never keeps
      pointing at a dead image URL.
    · POST /api/media/images/purge — deletes EVERY image in the folder.
      Requires `{confirm: "DELETE ALL"}` in the body as a server-side guard
      beyond the frontend's own typed-confirmation modal — the one truly
      destructive, no-undo action anywhere in this app.
    All three return 503 NOT_CONFIGURED if Cloudinary env vars aren't set,
    same pattern as the per-delegate photo routes in server.js.

package.json / package-lock.json
    Dependencies (express, pg, exceljs, cors, dotenv, @anthropic-ai/sdk,
    bcryptjs, jsonwebtoken, unpdf) and the scripts: "npm run dev",
    "npm start", "npm run seed:demo", "npm run reset:login",
    "npm run seed:team".

.env  (gitignored, not shown in file list)
    Holds DATABASE_URL for your Postgres connection, JWT_SECRET for
    signing session tokens (see auth.js above), and optionally
    OLLAMA_HOST / OLLAMA_MODEL / ANTHROPIC_API_KEY / ANTHROPIC_MODEL for
    the AI Insights feature. See .env.example for the full list with
    explanations.


FRONTEND/  (React + Vite SPA, port 5173)
------------------------------------------

Entry point & routing:

  main.jsx
      The actual entry point — mounts React to the DOM, wraps everything
      in BrowserRouter and LanguageProvider (the translation context),
      imports the global tokens.css.

  App.jsx
      The router. Decides auth state, and maps every URL path
      (/dashboard, /assistant, /mobile, etc.) to its page component.
      This is the file to check first when you want to know "what page
      does this URL show." Also remembers whether you were in the mobile
      section before logging out, so signing back in returns you there.
      /scanner → UnifiedScannerPage.jsx (2026-07-20 — desktop entrance-kiosk
      Face+QR scanner; see the pages list below).
      Per-page RBAC (2026-07-20): every desktop route except /settings,
      /guide and /accounts is now wrapped in <ViewGate perm="viewX">, a
      small local component that renders its children if
      getPermissions()[perm] is true, else redirects — see permissions.js's
      new "desktopView"/"mobileView" permission groups. Every /mobile/*
      route except /mobile/profile is wrapped the same way with
      mode="mobile". The redirect target is never hardcoded to /dashboard —
      firstAllowedRoute(perms, mode) walks DESKTOP_FALLBACK_ORDER /
      MOBILE_FALLBACK_ORDER (both defined just above ViewGate) and lands on
      the FIRST route this specific account is actually allowed, falling
      back to /settings or /mobile/profile (never gated) if literally
      nothing is. pickHomeRoute() (post-login landing, the index "/" route,
      and the catch-all "*") and handleSignIn() both now call this instead
      of a hardcoded "/dashboard"/"/mobile", so an account with Dashboard
      view unchecked doesn't land somewhere it immediately bounces from.
      /accounts stays gated on the manageAccounts ACTION permission, not a
      view toggle — only a real admin should grant/revoke everyone else's
      access.

  vite.config.js
      Dev server config — proxies /api/* calls to the backend on :4000
      so the frontend never needs to think about CORS.
      server.strictPort: true — without this, Vite silently falls back to
      the next free port (5174, 5175...) whenever 5173 is already held by a
      leftover process (this project's dev servers are prone to that — see
      the note on preview_stop leftovers elsewhere in this doc). That's not
      just cosmetic: Chrome's password manager and any browser-stored
      session scope credentials/state per-origin INCLUDING the port, so a
      "remember password" save made while running on :5173 silently stops
      autofilling the moment a later run drifts to :5174 — no error, it just
      looks like the remember-password feature is broken. strictPort makes a
      stale process on 5173 fail loudly instead, so it gets killed rather
      than routed around.
      HTTPS via @vitejs/plugin-basic-ssl (2026-07-21, pinned to ^1.1.0 — the
      latest major needs Vite 6, this project is on Vite 5). WHY: getUserMedia
      (camera) and the Credential Management API (password save/autofill)
      BOTH only work in a browser "secure context" — https://, or
      http://localhost specifically. A phone reaching the dev server over the
      LAN as http://192.168.1.11:5173 is a non-secure origin, so EVERY
      browser (Chrome, Brave, ...) silently blocks both — this was the real
      cause of "Camera unavailable" on-phone and passwords never offering to
      save, not a code bug in either feature. Now serves
      https://192.168.1.11:5173 with an auto-generated self-signed cert (one-
      time "Advanced → Proceed" browser warning on first visit from a new
      device). On the laptop, localhost was already secure — only the scheme
      changes there (https://localhost:5173).

Shared components (src/components/):

  Layout.jsx
      The desktop shell — renders Sidebar + whatever page is routed into
      the main content area. Also polls the backend every 15s to catch
      if your account got edited/deleted while logged in, and separately
      polls lib/exceptionsApi.js's getCriticalOpenCount() every 15s to
      drive the Sidebar's live "Exceptions" badge (used to be hardcoded
      demo state).
      • Collapsible sidebar (<=1024px, i.e. tablet and narrower): owns the
        sidebarOpen state, renders the .app-topbar-collapsed hamburger bar
        and the .app-sidebar-backdrop (click-to-close), and auto-closes the
        drawer on every route change. Above 1024px none of this renders
        visibly — the sidebar stays exactly as it always was (sticky,
        always open, no hamburger). See tokens.css's collapsible-sidebar
        block for the actual open/closed CSS.
      Renders <ChatBubble /> once alongside <Outlet/>, so the floating chat
      widget persists across every desktop route (see ChatBubble.jsx below).

  Sidebar.jsx
      The left nav rail — links to every page, the EN/中文 toggle, and
      the account/logout block at the bottom. The account block (avatar +
      name/role) links to /settings and has a hover highlight
      (.sidebar-account); the sun/moon dark-mode toggle sits right beside it.
      Takes an `open` prop from Layout.jsx (applies the .sidebar.open class);
      unused/ignored above the 1024px breakpoint.
      "Check-in" (→ /checkin, QRCheckInPage.jsx) and "Face + QR scan"
      (→ /scanner, UnifiedScannerPage.jsx) sit next to each other — Check-in
      was briefly removed 2026-07-20 then restored the same day (kept-but-
      hideable via viewCheckin instead of deleted — see QRCheckInPage.jsx's
      entry above), so its nav item and the QrCode icon import came back too.
      Per-page RBAC (2026-07-20): every nav item except Account control is
      now individually gated on its matching "desktopView" permission from
      permissions.js (perms.viewDashboard/viewTrips/viewDocuments/
      viewCheckin/viewScanner/viewExceptions), same conditional-spread
      pattern the Documents item already used for manageDelegates — an
      account with a view unchecked just doesn't see that nav item.
      MobileLayout.jsx's tabs list follows the identical pattern for
      /mobile/* using the "mobileView" permissions (Profile stays ungated).
      "Chat assistant" nav item removed — the AI Trip Assistant is now a
      floating bubble (ChatBubble.jsx), not a dedicated destination.
      "User guide" is NOT a primary nav item (moved 2026-07-20) — it's a
      small icon-only "?" (HelpCircle) link in the footer account row, next
      to the theme toggle, since it's a help/reference resource rather than
      a workflow destination.

  ChatBubble.jsx
      New. Floating chat widget, rendered once from Layout.jsx so it's
      available on every desktop route (replaces the old dedicated
      "/assistant" sidebar link + route). A fixed round FAB (bottom-right)
      toggles a ~380×560px floating panel containing
      `<ChatAssistantPage embedded />` — reuses Vance's existing chat engine
      completely unchanged (session history, streaming, delegate-name
      cards), just rendered inside a small floating container instead of
      its own full page.
      ChatAssistantPage.jsx (Vance's Screen 6, pages/ChatAssistantPage.jsx)
      gained an `embedded = false` prop for this: when true, skips the
      page-eyebrow/title/subtitle chrome and the 260px chat-history sidebar
      column (a floating bubble is a live-conversation surface, not a full
      chat-management UI — history/rename/pin/delete/export aren't removed,
      just not shown in the compact layout), and the conversation panel
      fills its container's height instead of a fixed 520px. No change to
      streaming/sending/delegate-card logic. App.jsx's `/assistant` route
      was removed entirely — nothing links there anymore, and the desktop
      Layout group's existing catch-all (`<Route path="*">` → pickHomeRoute())
      picks up any stray visit and redirects sanely.
      Follow-up bug (from a screenshot: visibly stuck, un-scrollable chat,
      plus an obviously duplicated "Trip assistant" header): the height
      chain was broken in embedded mode — the component's OUTERMOST wrapper
      had no defined height (`className={embedded ? undefined : "page"}`
      with no matching style), so every `height: "100%"` further down had
      nothing real to resolve against and just collapsed, and the messages
      list's `overflowY: auto` never had a constrained box to actually
      overflow within. Fixed by giving the outermost div
      `{ height: "100%", display: "flex", flexDirection: "column",
      minHeight: 0 }` when embedded, switching the (now single-column)
      middle wrapper from CSS grid to a plain flex column (`flex: 1,
      minHeight: 0` — grid's default row-sizing doesn't stretch to fill a
      flex parent the way a nested flex column does), same `flex: 1,
      minHeight: 0` on the conversation `.card`, and adding the missing
      `minHeight: 0` on the messages div itself (flex items default to
      `min-height: auto`, which silently prevents shrinking below content
      size and is a classic reason `overflow: auto` "does nothing" inside
      nested flexbox). Also hid the conversation panel's own internal header
      row (avatar/title/Ready-status/Export) when embedded — ChatBubble.jsx
      already renders its own header with the same title, so both stacking
      was a real visible duplicate, not by design.
      Scroll-reveal v1 (2026-07-18, superseded same day — see v2 below):
      "hide the chatbot until when i scroll down like typical uiux
      chatbot... it hard for me to click certain stuff" — the FAB was
      always visible and sat over other clickable UI near the top of pages.
      First pass: a `scrolled` state from a `window` scroll listener
      (`SCROLL_REVEAL_PX = 120`) that, once past the threshold, stayed
      visible indefinitely regardless of further scroll activity.
      Scroll-reveal v2 (2026-07-18, same day): flagged as still a defect —
      "stays permanently visible as soon as the user scrolls down, which
      blocks essential UI elements like the Delete buttons... should remain
      completely hidden by default, regardless of the page position." v1's
      `scrolled` boolean replaced with `recentlyActive`, driven by a scroll
      listener that (re)starts a 5000ms `setTimeout` on every scroll event
      and clears the flag when the timer fires uninterrupted — genuinely
      activity-based (auto-hides 5s after scrolling STOPS), not a one-way
      position threshold, and hidden by default before any scroll ever
      happens (no longer primed on mount). `visible = recentlyActive || open
      || !!dragPos` keeps the "stays visible once opened" exception the user
      asked for explicitly, plus stays visible mid-drag.
      Drag-to-corner (2026-07-18, same request): "Add drag-and-drop
      capability... users must be able to freely click-and-drag (or
      touch-and-drag on mobile) the chatbot button to any corner of the
      screen." Implemented with native Pointer Events
      (onPointerDown/onPointerMove/onPointerUp) rather than HTML5
      drag-and-drop or separate mouse/touch handlers, since Pointer Events
      unify mouse/touch/pen through one code path. A `DRAG_THRESHOLD_PX = 6`
      total-displacement check distinguishes a genuine drag from a plain
      click/tap (below = toggle open same as before; above = reposition —
      note a raw synthetic `.click()` bypasses this pointer-based
      interaction entirely, a real false-negative hit during testing; use a
      pointerdown+pointerup pair instead). On release the FAB snaps to
      whichever of the four screen quadrants the pointer ended in (not a
      free-form position — matches "any corner," not "anywhere"), persisted
      to localStorage (`mg_chatbubble_corner` on desktop,
      `mg_mobile_chat_corner` on mobile) so it survives a reload. The
      floating panel (desktop)/bottom sheet (mobile) repositions to open
      sensibly relative to whichever corner the FAB currently occupies.
      Same pattern applied to MobileChatBubble.jsx (below) with its own
      `mg-mobile-chat-fab` class and mobile-appropriate margins (bottom
      corners clear the fixed `.mobile-tabbar` + safe-area-inset).

  StatusBadge.jsx
      Small reusable pill component for status labels (Present/Missing/
      Unassigned/etc.) and confidence-score badges.

  DelegateAvatar.jsx
      Renders a delegate's photoUrl if they have one (POST/DELETE
      /api/delegates/:id/photo, see server.js + cloudinary.js), otherwise
      falls back to the existing initials-in-a-circle look — same size/shape
      either way, drops in anywhere a plain .avatar span was used.
      Status-tinted across all 5 states (2026-07-19 — was a MISSING-vs-
      everyone-else binary: red for MISSING, green for literally everything
      else, which meant an UNASSIGNED delegate's placeholder avatar
      incorrectly showed green, as if already accounted for). Exports a
      shared `statusTone(status)` helper — {fg, bg} CSS var pairs —
      UNASSIGNED gets `--st-neutral`/`--st-neutral-bg` (muted grey). At the
      time this was written, `--st-unassigned` (used for badges/KPI tiles
      elsewhere) was still amber, deliberately kept separate from this grey
      — that distinction is now moot: `--st-unassigned`'s own token VALUES
      were changed to the same grey shortly after (2026-07-19, direct
      request — see tokens.css), so both spellings render identically now,
      though the two CSS variables remain separate for architecture
      cleanliness. The other four (ASSIGNED/ARRIVED/LATE/MISSING) use the
      SAME tokens already used everywhere else in the app (StatusBadge,
      coach cards) — no new colors invented. Reads delegate.status — so any
      caller passing a
      delegate that carries status gets it for free (the "All delegates"
      table, the reverse-headcount modal, and Mobile attendance). The ring
      uses boxShadow, not border, so it never changes the box size / breaks
      table alignment. The Dashboard edit-delegate modal's large preview
      avatar imports and uses the same `statusTone()` off form.status (was
      its own separate MISSING-vs-PRESENT-only ternary, same bug). Used on
      the Dashboard's "All delegates" table and MobileAttendancePage.jsx.
      Photo upload/remove itself lives in the Dashboard's edit-delegate
      modal (create flow doesn't get one — a delegate needs to exist first).
      Desmond's Trips board (TripCoachPage.jsx's DelegateCard + the
      DelegateDetailPanel slide-out) shows the same photo too — inline
      <img className="tf-avatar"> there rather than this component, since
      that page's avatars use their own per-id color scheme when there's no
      photo. Required adding "photoUrl" to desmond.js's own GET/POST
      /api/delegates SELECT/RETURNING column list (it queries the shared
      delegates table directly with its own SQL, not through data.js).
      cloudinary.js's uploadImage() returns a delivery URL with
      f_auto,q_auto (auto format/compression) baked in, not the raw
      upload result — so every place a photo renders gets that for free.

  DelegateLocationMap.jsx
      Renders a delegate's "lastLocation" (see data.js) as a static embedded
      Google Map — a single <iframe> via the Maps Embed API, not live
      tracking. Needs VITE_GOOGLE_MAPS_API_KEY in frontend/.env (see
      .env_Future.example); degrades to a plain text line instead of a
      broken iframe when the key isn't set. Used by the Dashboard's
      create/edit delegate modal (live preview as you type) and the "All
      delegates" table's map-pin button (DashboardPage.jsx), and by
      MobileAttendancePage.jsx's per-row map-pin. The pin only appears when
      a delegate has a location set, and is tinted red for MISSING delegates.

  AnalyticsPanel.jsx
      Rendered inside the Dashboard's "Analytics" tab. Contains: the
      charts + "Customize" widget-toggle UI, and the "AI Insights" card
      with the "Generate Insights" button (calls backend/routes/insights.js,
      shows which source answered — local Ollama or Claude — and respects
      the current language). While a request is in flight, shows 3 shimmer
      skeleton bars (.skeleton-line, tokens.css) in the results area instead
      of leaving it blank. splitInsightPoints() now splits each returned
      line into a bold "Label:" (Overall/Missing/Advice) + body, matching
      insights.js's fixed 3-point prompt structure.
      Persistence fix (2026-07-19): the insights state (loading/result/
      source/error) used to be plain useState here — since this component
      only renders while the Analytics tab is active, switching Dashboard
      tabs (or navigating away entirely) unmounted it and threw the state
      away, so a request that finished AFTER you left looked like it had
      silently reset when you came back. Moved to lib/insightsStore.js, a
      module-level store (Map keyed by tripId + a subscriber list) OUTSIDE
      React's component lifecycle — the fetch and its result now live
      independent of whether anything is mounted to observe them. This
      component reads it via useSyncExternalStore instead of local state;
      generateInsights() here just calls the store's version. Verified live
      via genuine client-side navigation (not a hard reload, which would
      trivially "fix" this by resetting the whole JS runtime): Generate →
      immediately switch tabs → immediately navigate to a different route →
      wait there while the real Ollama request finishes in the background →
      navigate back → full result renders correctly.
      • 4th customizable widget: "Delegates onboarded over time" — a
        Recharts AreaChart (gradient-filled) showing cumulative delegate
        count by day, built purely on the frontend from the `delegates`
        prop's own `createdAt` timestamps (bucketed by day, running total
        carried forward across gap days) — no new backend endpoint. Takes
        a new `delegates` prop from DashboardPage.jsx. The two existing pie
        charts (Attendance breakdown / VIP vs. non-VIP missing) were
        already doughnut-style (innerRadius/outerRadius set).
      • "Filter" and "Sort" buttons (next to "Customize") — same collapsible-
        panel pattern, each its own toggle state. Filter: a coach <select>
        ("All coaches" or one specific coach) that scopes both "Coach load"
        and "VIP vs. non-VIP missing" to that coach (coachLoad filters
        data.coaches by id; vipMissing filters the `missing` array by
        coachId). Sort: a <select> reordering "Coach load"'s bars — by
        coach name (default), most boarded, most remaining capacity, or
        most missing (missing count per coach computed by grouping the
        `missing` array's coachId). Both are pure frontend reads over data
        already fetched by DashboardPage — no new endpoint or params. The
        "Coach load" card heading appends the coach's name when a filter is
        active (e.g. "Coach load — Coach 5").

  ExportModal.jsx
      Export configuration modal (added 2026-07-19), opened from the
      Dashboard's Export button. Loads GET /trips/:id/export/options to
      populate: status chips, coach chips (+ Unassigned), a 14-option column
      picker, a VIP-only toggle, an "AI summary sheet note" toggle, and a
      natural-language box whose "Apply" button POSTs /export/ai-filter and
      pre-ticks the resulting structured filters (fully editable — the AI is
      an assist, never authoritative). Download POSTs the whole config to
      /trips/:id/export and streams the .xlsx via a blob (raw fetch with the
      auth header, since window.open can't send it — same reason the old
      inline exporter used fetch+blob). See backend/routes/export.js for the
      workbook + AI-filter server side. Replaced DashboardPage's old
      one-click exportXlsx().
      "Workbook language" selector (2026-07-19): English/中文 buttons at the
      top of the modal, defaulting to (but independently overridable from)
      the app's own current UI language — sent as `lang` in the export POST
      body, which routes/export.js now actually uses to translate the whole
      generated workbook (previously sent but silently ignored server-side).
      Also surfaced — while live-testing this — that NONE of this modal's
      own UI strings had ever had Chinese translations added to i18n.jsx's
      DICT (falling back to raw English even with the app in 中文 mode);
      added the missing ~20 keys, including one ("Last location") found only
      by testing the actual rendered modal in Chinese after the first batch.

  LogExceptionModal.jsx
      Jayden — the "Log exception" create form (issue type, delegate
      picker, note, "Mark as critical" toggle). Only reachable via the
      gated "Log exception" button on ExceptionInboxPage.jsx.
      Issue type includes "Others" (free-text label input appears inline,
      capped at TYPE_OTHER_MAX/20 chars, required before submit). Below the
      Critical toggle, a Normal/Low priority button pair (colour-matched to
      the inbox pills) sets the ticket's priority when Critical is off —
      superseded/disabled while Critical is on. Cherry-picked from Jayden's
      upstream branch with every t()/useLang() call preserved by hand (his
      branch had dropped i18n entirely on this file — see the note near the
      bottom of this file).

Library helpers (src/lib/):

  api.js
      The HTTP client — apiGet/apiPost/apiPatch/apiDelete, all
      auto-attaching your JWT. Also handles token/user storage (login
      persistence).

  insightsStore.js
      New (2026-07-19). AI Insights state (loading/result/asOf/source/error)
      as a module-level store, keyed by tripId — NOT React state, deliberately,
      so it survives AnalyticsPanel.jsx being unmounted (switching Dashboard
      tabs) or the whole app navigating away entirely. subscribe()/getSnapshot()
      pair for useSyncExternalStore; generateInsights(tripId, lang) starts the
      POST /api/trips/:id/insights request and updates the store regardless of
      whether any component is currently mounted to observe it.

  i18n.jsx
      The translation system — the English/Chinese dictionary,
      useLang() hook (returns { lang, toggleLang, t }), and language
      toggle logic. Also exports translate() for code outside React
      rendering (e.g. window.alert/confirm messages). Static dictionary
      only — no data ever sent to a translation API, so delegate
      names/live data are never exposed.

  theme.jsx
      App-wide light/dark theme — ThemeProvider + useTheme() hook (returns
      { theme, toggleTheme }), persisted to localStorage (mg_theme), stamps
      data-theme="dark" on <html>. tokens.css's :root[data-theme="dark"]
      block overrides the neutral/surface/status-tint variables everything
      (.card/.btn/.input/the sidebar/etc.) is built on, so one toggle
      re-themes every page. TripsListPage's useTfTheme() is now just a thin
      wrapper around this (no more separate tf_theme localStorage key), so
      the Trips/Coach board pages follow the same toggle too.
      Native form widgets fix (2026-07-18): tokens.css never set the CSS
      `color-scheme` property, so native controls (<select> dropdown popups,
      <input type="time">, checkboxes, scrollbars) kept rendering their LIGHT
      system chrome regardless of our own dark CSS variables — read to users
      as random black text on a white popup ("update the dark theme change
      for onboarding page. It show black text instead of white"). Added
      `color-scheme: light` to :root and `color-scheme: dark` to
      :root[data-theme="dark"] in tokens.css. A second, narrower contributor
      on the same page: BoardingPassesView.jsx's .mg-passrow is a raw
      <button> with no explicit `color` — unstyled <button> text defaults to
      a browser system color independent of inherited CSS — fixed with an
      explicit `color: var(--ink)` on that class.

  claudeParse.js
      Vance's parse/confirm bridge for the Onboarding page — uploads a file
      to backend/routes/vance.js (POST /api/documents/parse), polls the
      async parse job, and commits the reviewed rows (onboarding/confirm).
      Real backend now, not a demo.

  faceScan.js
      NEW (2026-07-21). Shared face-scan helpers — the PDPA zero-image
      vectorizer (vectorizeFaceLandmarks: draws one off-DOM frame, folds it
      into an irreversible token, then zeroes the raw pixel buffer in place),
      the biometric-token validator (isValidBiometricToken), and the ">1s
      SLA missed" error tone (playErrorTone). Extracted so the desktop
      scanner (UnifiedScannerPage.jsx) and the mobile scanner (MobileScanner
      Page.jsx) share ONE copy instead of each keeping their own — the
      logic originally lived (duplicated) inside those pages.

  exceptionsApi.js
      Jayden's data layer for the Exception Inbox — built on the shared
      apiGet/apiPost/apiPatch/apiDelete (inherits token storage/typed
      errors for free) plus subscribeStream() for the live SSE channel
      (EventSource, token passed as ?token= since it can't set headers)
      and getCriticalOpenCount() (used by Layout.jsx's sidebar badge).
      checkInByQR() (POST /api/checkins/qr) was added for the /checkin
      screen's QR pill, but QRScannerPanel.jsx no longer calls it — see the
      QRScannerPanel.jsx entry below for why (unused, left in place).
      TYPE_OTHER_MAX/PRIORITIES/BASE_PRIORITIES/issueLabel() added — mirrors
      backend/routes/exceptions.js's "Others" type + 3-tier Critical/Normal/
      Low priority. issueLabel(ticket) returns the ticket's custom typeOther
      label when set, else the ISSUE_LABEL[type] fallback (kept exported,
      still used as issueLabel()'s own fallback). createException() gained
      typeOther/priority params; markCritical is still honoured for backward
      compatibility (always forces CRITICAL when passed).

  useSessionGuard.js
      Shared hook (Layout.jsx + MobileLayout.jsx): polls GET /api/auth/
      session every 15s + on window focus, force-logs-out on a 401 (token
      invalidated — renamed/deleted account, or a newer login elsewhere
      bumped token_version) or a permissions/username change. Extracted
      from Layout.jsx so mobile gets the same behavior — see the note on
      MobileLayout.jsx above for why that mattered.

Pages (src/pages/) — one file per route:

  LoginPage.jsx            /login              Sign-in form. Two explicit
                                                submit buttons — "Sign in"
                                                and "Login for Mobile" — call
                                                onSignIn("desktop"/"mobile")
                                                in App.jsx; which UI you land
                                                in is no longer guessed from
                                                screen width (that auto-detect
                                                was removed). Also owns the
                                                "Forgot password?" flow
                                                (ForgotPasswordModal in this
                                                same file): username + new
                                                password + confirm, calls
                                                POST /api/auth/reset-password.
                                                No identity verification
                                                beyond the username existing —
                                                see the comment on
                                                resetPassword() in data.js;
                                                fine for this project's small
                                                trusted-team context, NOT
                                                production-safe as-is.
                                                Now imports useTheme() and
                                                renders a sun/moon toggle
                                                beside the EN/中文 button
                                                (top-right); fixed the dark-
                                                theme card/modal — S.card and
                                                the ForgotPasswordModal's
                                                S.modal used a hardcoded
                                                background: "#fff" so dark
                                                mode's near-white --ink text
                                                rendered nearly invisible on
                                                a still-white card — both now
                                                use var(--surface).
                                                The Staff ID/password inputs
                                                used to sit in plain <div>s
                                                with no <form> and no name/
                                                autoComplete attributes —
                                                submission only ever happened
                                                via onClick handlers or a
                                                manual Enter-key listener, so
                                                the browser's own "save
                                                password?" prompt never had
                                                anything to hook into and
                                                never fired (document.forms
                                                was empty). Wrapped the inputs
                                                in a real <form
                                                onSubmit={e=>{e.preventDefault();
                                                submit("desktop")}}>, added
                                                name="username"/
                                                autoComplete="username" and
                                                name="password"/
                                                autoComplete="current-password",
                                                and changed "Sign in" to
                                                type="submit" (the "Remember
                                                password" checkbox and "Login
                                                for Mobile" stay type="button"
                                                — mobile mode still calls
                                                submit("mobile") directly, it
                                                just isn't the form's default
                                                submit path). This is separate
                                                from the app's OWN "keep me
                                                signed in" checkbox below,
                                                which controls whether the JWT
                                                goes to localStorage vs
                                                sessionStorage — that logic is
                                                unchanged; this fix only makes
                                                the browser's native
                                                credential-save prompt appear.
                                                That got the SAVE prompt
                                                showing (confirmed by the
                                                user), but a follow-up report
                                                — saved once, then didn't
                                                autofill on a later visit —
                                                traced to vite.config.js not
                                                pinning the dev server port
                                                (see its own doc entry) rather
                                                than anything in this file.
                                                A THIRD report — right after
                                                logging out, the password
                                                wasn't actually saved at all —
                                                led to a more direct fix:
                                                Chrome's heuristic detection
                                                (watch the form submit, infer
                                                success from the page
                                                changing) is unreliable in an
                                                SPA like this one, since there
                                                is no real full-page
                                                navigation for it to key off.
                                                submit() now also calls the
                                                Credential Management API
                                                directly — `new
                                                window.PasswordCredential(...)`
                                                + `navigator.credentials.
                                                store()` — right after a
                                                successful login, gated on the
                                                "Remember password" checkbox
                                                and feature-detected
                                                (window.PasswordCredential;
                                                Chrome/Edge/Brave support it,
                                                Firefox/Safari don't, wrapped
                                                in try/catch so it's never
                                                fatal to login either way).
                                                This tells the browser
                                                directly "save this
                                                credential" instead of relying
                                                on it to correctly guess from
                                                page-change heuristics.
"Keep me signed in" was briefly renamed to
                                                "Stay signed in" with the
                                                plaintext-password-remembering
                                                code removed (a real security
                                                gap — anything with JS access
                                                to this origin could read a
                                                saved password), then restored
                                                to "Remember password" at the
                                                user's explicit request after
                                                being flagged. mg_remember_v2
                                                in localStorage again stores
                                                {staffId, password} in plain
                                                text when ticked, prefilling
                                                the form on return — see the
                                                comment on readRemembered() in
                                                this file; documented as fine
                                                for a school demo/small
                                                trusted team, NOT
                                                production-safe.
                                                "Quick Scanner Access" button —
                                                added 2026-07-21 as a third
                                                submit path requiring a real
                                                login (submit("mobile", "/mobile/
                                                scanner")) landing on the
                                                authenticated mobile scanner;
                                                changed the SAME day, once the
                                                user explicitly asked for it to
                                                be passwordless, to a PLAIN
                                                navigate("/kiosk-scan") that
                                                never touches the Staff ID/
                                                password fields or the login
                                                API at all — see Kiosk
                                                ScannerPage.jsx (pages/
                                                KioskScannerPage.jsx) for why
                                                that's still safe (its own
                                                narrowly-scoped, server-side-
                                                enforced kiosk token). The
                                                earlier `dest` parameter
                                                threaded through submit()/
                                                onSignIn()/App.jsx's
                                                handleSignIn() for the first
                                                version was reverted along
                                                with it, since nothing calls it
                                                anymore.
  DashboardPage.jsx        /dashboard          Main admin screen — KPI tiles,
                                                coach status, History tracker,
                                                delegate CRUD table,
                                                Delegate/Analytics tabs (was
                                                "Overview", renamed to
                                                "Delegate" — internal tab-state
                                                key is now "delegate" not
                                                "overview"; Analytics tab
                                                renders AnalyticsPanel.jsx).
                                                All-delegates table filter bar
                                                (2026-07-21): gained a coach-
                                                assignment <select> (coachFilter
                                                state) alongside the existing
                                                search + status filter + sort —
                                                options are the live coach list
                                                plus an explicit "Unassigned"
                                                (delegates with no coachId),
                                                matching the mobile Attendance
                                                page's coach filter.
                                                • No standalone "Missing right
                                                  now" TABLE on the Delegate
                                                  tab anymore — the KPI tile
                                                  still shows the count; that
                                                  old flat list was removed as
                                                  redundant with "All
                                                  delegates".
                                                • "Reverse headcount" — NOT a
                                                  tab anymore. Was briefly its
                                                  own persistent Dashboard tab;
                                                  the user liked the content
                                                  but not that UX ("hide the
                                                  reverse headcount tab, i want
                                                  it to display when i click a
                                                  button"), so it's now an
                                                  on-demand modal
                                                  (`headcountOpen` state)
                                                  opened via a "Reverse
                                                  headcount" button in the
                                                  "Coach status" card header,
                                                  using the same overlay/modal
                                                  pattern (S.overlay/S.modal)
                                                  as the create/edit delegate
                                                  and map-preview modals.
                                                  Content: one card per coach
                                                  (boarded/capacity progress
                                                  bar, "Arrived" badge or "N
                                                  missing"), each listing its
                                                  still-unaccounted-for
                                                  delegates (avatar, VIP flag,
                                                  last known location) with the
                                                  same map-pin → DelegateLocat
                                                  ionMap popup pattern as "All
                                                  delegates". Its missing-list
                                                  filter is `status ===
                                                  "MISSING" || status ===
                                                  "LATE"` (not the getMissing()
                                                  array, which is MISSING-only
                                                  — this modal reads straight
                                                  off the full `delegates`
                                                  list instead so LATE
                                                  delegates show up here too).
                                                  The button that opens this
                                                  modal was renamed "Reverse
                                                  headcount" → "More details"
                                                  (2026-07-19) for UX clarity.
                                                • Coach status logic
                                                  (2026-07-19, went through two
                                                  iterations same day): first
                                                  pass added a 3-way severity
                                                  badge (Missing ▸ Late ▸ All
                                                  in, with a separate orange "N
                                                  late" badge) — the user then
                                                  asked to simplify back to TWO
                                                  states, so LATE now folds
                                                  into the same bucket as
                                                  MISSING with no separate Late
                                                  badge: a coach reads
                                                  "Arrived" only when every
                                                  assigned delegate has
                                                  actually boarded, and
                                                  "Missing" whenever it holds
                                                  ANYONE not yet accounted for
                                                  (`attention = missing +
                                                  late`). Same logic on desktop
                                                  CoachBar, the More details
                                                  modal (above), and mobile
                                                  MobileHomePage.jsx's coach
                                                  rows. Backed by a per-coach
                                                  `late` count added to
                                                  getDashboard (data.js) — the
                                                  same pass ALSO fixed
                                                  getDashboard counting
                                                  `present`/`boarded` as
                                                  PRESENT-only (undercounted
                                                  every ARRIVED delegate after
                                                  the check-in migration) — now
                                                  an isBoarded() helper counts
                                                  PRESENT+ARRIVED. Mobile's
                                                  per-coach row tap target
                                                  changed from a hardcoded
                                                  `status=MISSING` link to
                                                  `status=ALL` — with Late
                                                  folded into the badge's
                                                  "missing" count, a
                                                  MISSING-only link could land
                                                  on an empty list for a
                                                  late-only coach.
                                                • Missing-delegate subtitle
                                                  (2026-07-19): the More
                                                  details modal used to show
                                                  each delegate's `lastSeen`
                                                  free-text note, falling back
                                                  to a status-sounding string
                                                  ("No status", later "No last
                                                  seen recorded") that read as
                                                  if the delegate had no status
                                                  at all. Changed to show
                                                  `lastLocation` instead
                                                  (falling back to the label
                                                  "Last known location" itself
                                                  when none is recorded) —
                                                  matches what the map-pin
                                                  button right next to it
                                                  already opens, and is what
                                                  this screen is actually for.
                                                • "Open exceptions" KPI tile
                                                  REPLACED (2026-07-19) with a
                                                  "Late" tile (Clock icon,
                                                  --st-late orange, backed by
                                                  the new kpis.late field) —
                                                  the exceptions-count KPI and
                                                  its /exceptions link (added
                                                  earlier the same day) were
                                                  both removed along with it —
                                                  exceptions are still fully
                                                  reachable via the sidebar,
                                                  just not from this tile.
                                                • All 4 KPI tiles clickable
                                                  (2026-07-19, follow-up pass
                                                  the same day): Missing/
                                                  Present/Unassigned got the
                                                  same drill-down behavior Late
                                                  already had — the one-off
                                                  showLateDelegates() was
                                                  generalized into
                                                  showDelegatesFiltered(status),
                                                  which switches to the
                                                  Delegate tab, sets the status
                                                  filter (MISSING/ARRIVED/
                                                  UNASSIGNED/LATE), and scrolls
                                                  the delegate table
                                                  (delegateTableRef) into view.
                                                • Tile order (2026-07-19,
                                                  follow-up): Missing, Late,
                                                  Present, Unassigned (was
                                                  Missing, Present, Unassigned,
                                                  Late) — groups the two
                                                  "needs attention" statuses
                                                  together up front. Mobile
                                                  MobileHomePage.jsx's 3-tile
                                                  row reordered the same way:
                                                  Missing, Late, Present.
                                                • KPI grid layout fix
                                                  (2026-07-19): the tiles used
                                                  `repeat(auto-fit,
                                                  minmax(210px,1fr))`, whose
                                                  fluid track produces an
                                                  uneven trailing card width/
                                                  gap depending on exact
                                                  container width — read as
                                                  "unaligned." Replaced with an
                                                  explicit `.kpi-grid` class:
                                                  4 equal columns, breakpoints
                                                  to 2 at <=960px and 1 at
                                                  <=480px, `min-height:132px`
                                                  on every card so they're
                                                  identically sized at every
                                                  width (not just per-row).
                                                • KPI hover-color bug fix
                                                  (2026-07-19): `.kpi-clickable
                                                  :hover`/`:focus-visible`
                                                  hardcoded `var(--st-normal)`
                                                  (purple) — a leftover from
                                                  when this rule was written
                                                  for the single old "Open
                                                  exceptions" tile — so every
                                                  OTHER clickable card,
                                                  including Late, incorrectly
                                                  flashed purple on hover
                                                  instead of its own color.
                                                  Fixed by setting
                                                  `--kpi-tone: var(--st-
                                                  ${tone})` inline per Kpi
                                                  instance and having the
                                                  hover/focus rules reference
                                                  `var(--kpi-tone)` instead.
                                                • Mobile KPI restructuring
                                                  (2026-07-19, two passes same
                                                  day): first pass made the
                                                  strip a 2x2 grid (Missing/
                                                  Present/Late/Total). Second
                                                  pass dropped Total from the
                                                  tile row entirely — back to
                                                  a clean 3-across Missing/
                                                  Present/Late — and moved
                                                  Total to a small "Total
                                                  delegates: N" text line
                                                  directly under the page
                                                  title/greeting instead, so
                                                  the tile row stays dedicated
                                                  to actionable/at-risk
                                                  statuses. Same /mobile/
                                                  attendance?status=LATE link.
                                                • Export button now opens
                                                  ExportModal.jsx (configurable
                                                  multi-sheet export — see that
                                                  component + routes/export.js)
                                                  instead of the old one-click
                                                  blob download (removed).
                                                • "Staff operations" tab —
                                                  admin-only (manageAccounts),
                                                  gated the same as Account
                                                  control since it's about who
                                                  is using the app, not
                                                  delegate attendance. Was
                                                  briefly its own page/route
                                                  (/operations) but the user
                                                  asked for it to live on this
                                                  same page so admins don't
                                                  have to leave the Dashboard
                                                  — folded in as a 4th tab,
                                                  route removed. Two KPI tiles
                                                  (Active now / Total staff
                                                  accounts) + a live "Active
                                                  sessions" list (avatar,
                                                  name, role, "Ns/Nm ago").
                                                  "Active" = an account whose
                                                  last_seen_at (see data.js) is
                                                  within the last 45s. Only
                                                  polls GET /api/staff/active-
                                                  sessions every 2s (was 15s —
                                                  tightened so a login/logout
                                                  elsewhere shows up here
                                                  almost immediately; still
                                                  scoped to only fire while
                                                  this tab is open AND the
                                                  account has manageAccounts,
                                                  so no wasted load for anyone
                                                  else) while this tab is
                                                  open, and non-admins never
                                                  see the tab button at all.
                                                  Admin/staff breakdown
                                                  (2026-07-19): "Total staff
                                                  accounts" now shows a second
                                                  line — "N Admin · N Staff" —
                                                  backed by adminCount/
                                                  staffCount added to GET
                                                  /api/staff/active-sessions'
                                                  response (server.js filters
                                                  the already-fetched
                                                  listAccounts() by role, no
                                                  new query).
                                                  The top "Refresh" button now
                                                  also re-fetches this data
                                                  when the Staff operations tab
                                                  is active (it previously only
                                                  refreshed dashboard/missing/
                                                  delegates/activity, so
                                                  clicking Refresh here did
                                                  nothing until the next 15s/2s
                                                  poll tick or a full page
                                                  reload — loadStaffOps() was
                                                  pulled out of the polling
                                                  effect into its own function
                                                  so both call it). An explicit
                                                  logout also clears the
                                                  account's last_seen_at
                                                  server-side (see server.js's
                                                  POST /api/auth/logout above)
                                                  so it drops off this list
                                                  immediately instead of
                                                  lingering for up to 45s.
                                                • "All delegates" caps to 10
                                                  rows with a "Show all"
                                                  toggle, has a search box, an
                                                  "Uploaded" column
                                                  (delegates.createdAt), a
                                                  "Created by" column
                                                  (delegates.createdBy — the
                                                  creating account's display
                                                  NAME at creation time, a
                                                  snapshot not a live FK, so it
                                                  stays correct even if that
                                                  account is later renamed/
                                                  deleted; NULL/"—" for rows
                                                  predating this column or
                                                  created by a route that
                                                  doesn't pass an actor),
                                                  status filter + sort, and a
                                                  map-pin button per row (only
                                                  shown when that delegate has
                                                  a lastLocation) opening a
                                                  small DelegateLocationMap
                                                  popup — tinted red for
                                                  MISSING delegates. Coach
                                                  status caps
                                                  to 4 with "Show all", now
                                                  pushed to the bottom of its
                                                  card (flex column +
                                                  marginTop:"auto") instead of
                                                  sitting right under a short
                                                  list, so its position matches
                                                  History tracker's.
                                                • Per-row checkboxes + a
                                                  "Delete selected (N)" button
                                                  (only rendered once 1+ rows
                                                  are checked) sit alongside
                                                  the original blanket "Delete
                                                  all" button (was briefly
                                                  replaced by "Delete selected"
                                                  alone, then explicitly asked
                                                  back — both now coexist).
                                                  "Delete selected" deletes
                                                  just the checked ids via the
                                                  existing single DELETE /api/
                                                  delegates/:id route
                                                  (Promise.allSettled loop, no
                                                  new backend route); "Delete
                                                  all" still calls DELETE
                                                  /api/trips/:id/delegates
                                                  (wipes every delegate in the
                                                  current trip, unchanged from
                                                  before). The header checkbox
                                                  scopes "select all" to
                                                  exactly the rows currently
                                                  rendered (rowsShown —
                                                  respects the "Show all"
                                                  toggle, so it won't silently
                                                  select filtered-out or
                                                  not-yet-shown rows).
                                                  Selection clears when the
                                                  trip switcher changes.
                                                • Dashboard now resets window
                                                  scroll to the top once on
                                                  mount (an SPA route re-entry
                                                  otherwise restores the
                                                  browser's previous scroll
                                                  position — landing back on
                                                  this long page scrolled to
                                                  the bottom instead of a fresh
                                                  top-of-page view). Chat
                                                  assistant's OWN scroll-to-
                                                  latest-message behavior is a
                                                  separate mechanism — see
                                                  ChatAssistantPage.jsx below;
                                                  it was later found to have
                                                  the OPPOSITE bug (forcing a
                                                  page-level scroll on that
                                                  page) and fixed too.
                                                • "History tracker" (was "Live
                                                  activity") — persisted to the
                                                  activity_log table (GET/DELETE
                                                  /api/activity), not the old
                                                  in-memory 8-entry array, so it
                                                  survives backend restarts.
                                                  No longer caps with a "Show
                                                  all" button — the list itself
                                                  scrolls (maxHeight:360,
                                                  overflowY:auto) so every entry
                                                  is reachable. Per-row delete +
                                                  "Clear all", both gated on
                                                  manageDelegates.
                                                • Create/edit delegate modal has
                                                  a "Last known location"
                                                  field (delegates.lastLocation,
                                                  now REQUIRED when status is
                                                  Missing — saveForm() blocks
                                                  submit with an inline error
                                                  if left blank) separate from
                                                  "Last seen" (which is a
                                                  time/note, not an address) —
                                                  shows a live
                                                  DelegateLocationMap preview
                                                  as you type. Both fields only
                                                  appear in the modal when
                                                  status is Missing; saving
                                                  with any other status clears
                                                  both, so switching away from
                                                  Missing doesn't silently
                                                  carry stale data forward.
                                                  S.modal now has
                                                  maxHeight/overflowY:auto (the
                                                  modal previously had no
                                                  internal scroll, so adding
                                                  the map made it taller than
                                                  the viewport with no way to
                                                  reach the Save button).
                                                • The "All delegates" table's
                                                  map-pin now renders on EVERY
                                                  row (not just when a location
                                                  is set) but is disabled/greyed
                                                  (opacity 0.35) unless that
                                                  delegate is Missing — clicking
                                                  a Missing delegate's pin with
                                                  no location recorded yet shows
                                                  "No location has been
                                                  recorded..." instead of a
                                                  blank map. Same pattern on
                                                  MobileAttendancePage.jsx.
                                                • Trip switcher (its own
                                                  "Viewing trip" bar under the
                                                  header, fixed position): a
                                                  dropdown of all trips (from
                                                  /api/all-trips) re-points the
                                                  WHOLE dashboard to the chosen
                                                  trip. The base Beijing trip
                                                  ("t-1") is the unfiltered
                                                  view (every delegate); any
                                                  other trip scopes the
                                                  dashboard to just that trip.
                                                  Every trip is now editable —
                                                  create/edit/delete/export are
                                                  gated on permissions only
                                                  (manageDelegates/exportData),
                                                  not on which trip is selected.
                                                  Backend scopes by trip via
                                                  resolveTripUuid() in data.js.
                                                • Clicking a coach in Coach
                                                  status opens that trip's board
                                                  on the Trips page
                                                  (/trips?tripId=<uuid>).
                                                • Auto-refreshes every 2s (was
                                                  a one-time load on mount/trip
                                                  switch, plus the manual
                                                  Refresh button) so a change
                                                  made by another signed-in
                                                  staff member — a status edit,
                                                  a new upload, a coach
                                                  reassignment — shows up
                                                  without needing to click
                                                  Refresh. load() is guarded by
                                                  a loadingRef so an overlapping
                                                  poll tick is skipped rather
                                                  than racing a slow response;
                                                  safe against the create/edit
                                                  delegate modal, whose form
                                                  state is a separate local
                                                  copy seeded once when it
                                                  opens.
  TripCoachPage.jsx        /trips              Desmond's Trips & Coaches board.
                                                Renders TripsListPage (the trip
                                                grid) when no ?tripId= is set,
                                                or the per-trip fleet/itinerary
                                                board when one is. Styled by
                                                TripCoachPage.css; calls
                                                backend/routes/desmond.js.
                                                • The per-trip board's fetchAll()
                                                  (summary/coaches/itinerary/
                                                  delegates/activity) now polls
                                                  every 2s instead of loading
                                                  once on mount — the old
                                                  separate 15s activity-only
                                                  poll was folded in/removed
                                                  since fetchAll already covers
                                                  activity. Same "another staff
                                                  member's change shows up
                                                  automatically" fix as the
                                                  Dashboard.
  TripsListPage.jsx        (/trips grid)       Desmond — the trip grid + the
                                                shared useTfTheme dark-mode hook.
  OnboardingPage.jsx       /onboarding         Vance's DocuSync AI — two tabs
                                                (a third, "Scan to board" /
                                                ScanToBoardView.jsx, was
                                                dropped, and that dead file
                                                deleted 2026-07-19 — see below).
                                                • "Assign to trip" used to be a
                                                  HARDCODED stub array (t-1/
                                                  t-2/t-3 — only t-1 was ever a
                                                  real trip), totally
                                                  disconnected from the real
                                                  16-trip database — silently
                                                  created trip_id-NULL
                                                  delegates for anything but
                                                  t-1. Now fetches the REAL
                                                  trip list via getTrips()
                                                  (claudeParse.js, wraps GET
                                                  /all-trips), showing each
                                                  trip's dateRange alongside
                                                  its name. A stale localStorage
                                                  trip selection from before
                                                  this fix is detected and
                                                  cleared once the real list
                                                  loads.
                                                • Row editor redesigned from a
                                                  <table> to per-delegate
                                                  cards (.mg-drow): identity +
                                                  contact-detail icons
                                                  (Mail/Phone/Globe) shown
                                                  read-only by default, an
                                                  explicit per-row edit toggle
                                                  (editKeys Set, was tied to
                                                  needsReview before) opens
                                                  inline fields. "Already in
                                                  trip"/"Needs review" render
                                                  as small pills instead of a
                                                  whole-row background tint.
                                                • onboarding/confirm now
                                                  guards against junk rows:
                                                  isPlausibleDelegate() (in
                                                  vance.js) requires a name
                                                  with >=2 letters, and treats
                                                  a <=2-char single-token Latin
                                                  name (e.g. "jq", a stray test
                                                  entry) with no company/role/
                                                  email/phone/passport as junk
                                                  — CJK names are exempt from
                                                  the length check (2 chars is
                                                  a complete name there). The
                                                  endpoint returns
                                                  skippedInvalid alongside
                                                  added; the confirm alert
                                                  reports duplicates and
                                                  invalid rows separately.
                                                  Also now 404s with
                                                  UNKNOWN_TRIP instead of
                                                  silently creating trip_id-
                                                  NULL delegates when the
                                                  trip can't be resolved —
                                                  this surfaced a real latent
                                                  bug in data.js's
                                                  resolveTripUuid(): a
                                                  malformed/non-uuid tripId
                                                  threw a Postgres "invalid
                                                  input syntax for type uuid"
                                                  error (500) instead of
                                                  returning null like any
                                                  other non-match, because
                                                  Postgres validates the
                                                  parameter's shape against
                                                  the uuid_id column's type
                                                  before comparing values.
                                                  Fixed with a UUID_RE regex
                                                  pre-check — benefits every
                                                  OTHER caller of
                                                  resolveTripUuid() too
                                                  (server.js's dashboard/
                                                  missing/delegates/
                                                  deleteAll/create routes),
                                                  which would have hit the
                                                  same crash on a malformed
                                                  :id param.
                                                • Tab border-radius
                                                  (2026-07-18): the two tab
                                                  buttons ("Document parsing"/
                                                  "Boarding passes") had
                                                  `borderRadius: 0` — changed
                                                  to `"10px 10px 0 0"` for the
                                                  standard rounded-top tab
                                                  shape.
                                                "Document parsing" (default):
                                                upload a directory / attendee
                                                list / scanned passport, AI
                                                extracts delegate rows
                                                (confidence-scored, editable),
                                                confirm to add them to the
                                                shared list — each gets a QR
                                                boarding-pass token.
                                                "Boarding passes"
                                                (BoardingPassesView.jsx):
                                                redesigned from a QR-tile grid
                                                (unusable much past ~20
                                                delegates) to a searchable,
                                                filterable (All/Not boarded/
                                                Boarded), per-coach-grouped
                                                list — tap one delegate to
                                                see/copy/print their pass in a
                                                modal. "Print all" now respects
                                                the active search/filter,
                                                relabeling to "Print filtered
                                                (N)" when one is active,
                                                instead of always printing the
                                                whole trip regardless of what
                                                you were looking at.
                                                STATUS_META (boarded/not-
                                                boarded/no-coach label+color)
                                                only knew the original 3
                                                statuses (PRESENT/MISSING/
                                                UNASSIGNED) — a delegate set to
                                                the newer ARRIVED (see the
                                                5-status plan) fell through to
                                                the UNASSIGNED default and
                                                showed "No coach" even with a
                                                real coach and a genuine
                                                boarded status. Added ARRIVED
                                                (boarded, same as PRESENT) and
                                                ASSIGNED/LATE (not-boarded)
                                                entries; the boarded/pending
                                                filter tabs got the same
                                                PRESENT-or-ARRIVED check.
                                                vance.js's GET /onboarding/
                                                badges had the identical bug in
                                                its own `present` count
                                                (`status === "PRESENT"` only)
                                                — fixed there too.
                                                "Scan to board" (formerly the
                                                third tab, ScanToBoardView.jsx)
                                                was dropped — it's redundant
                                                with the shared Check-in page
                                                (QRCheckInPage.jsx + Jayden's
                                                QRScannerPanel), which is the
                                                actual on-site scanner;
                                                Onboarding's job is only to
                                                ISSUE passes. ScanToBoardView.jsx
                                                sat unused on disk for a while
                                                after, then was DELETED
                                                2026-07-19 as dead code (it was
                                                the only importer of the
                                                html5-qrcode npm package, which
                                                was uninstalled at the same
                                                time — the live scanner uses
                                                jsQR, not html5-qrcode).
                                                The route + sidebar item stay
                                                gated behind manageDelegates
                                                (parsing bulk-creates
                                                delegates); Boarding passes
                                                itself needs only
                                                requireAuth() on the backend.
                                                Calls backend/routes/vance.js.
  ExceptionInboxPage.jsx   /exceptions         Jayden's exception inbox —
                                                All/Critical/Open/Resolved
                                                tabs, a live critical-alert
                                                banner, manual attendance
                                                override, real-time updates
                                                via SSE (exceptionsApi.js).
                                                "Log exception"/Resolve/
                                                Override/Delete need the
                                                manageExceptions permission;
                                                viewing the inbox doesn't.
                                                Styled by
                                                ExceptionInboxPage.css.
                                                Priority badge is StatusBadge
                                                (already supported LOW/
                                                "Low" — badge-neutral — before
                                                this feature was wired up end
                                                to end, so it needed no
                                                change); issue-type label
                                                switched from a raw
                                                ISSUE_LABEL[type] lookup to
                                                issueLabel(ticket) so an
                                                "Others" ticket's custom
                                                typeOther text displays
                                                (still passed through t() —
                                                a no-op for untranslated free
                                                text, since t() falls back to
                                                the original string).
  ChatAssistantPage.jsx    /assistant          Vance's Trip Assistant — a real
                                                chatbot answering over the live
                                                trip snapshot (everyone's data),
                                                replies streaming token-by-token
                                                via SSE; saved/renameable/
                                                pinnable/exportable chats,
                                                clickable delegate names. Any
                                                signed-in user. Calls
                                                backend/routes/vance.js.
                                                Auto-scroll-to-latest-message
                                                sets messagesRef.current.
                                                scrollTop = scrollHeight
                                                directly on the message-list
                                                div — NOT endRef.
                                                scrollIntoView() (removed),
                                                which walks up through every
                                                ancestor scroll container it
                                                finds, including the outer
                                                page. On a tall message list —
                                                and worse once the app-wide
                                                110% zoom made the page taller
                                                than the viewport — that meant
                                                simply opening this page could
                                                yank the WHOLE page down to
                                                bring the chat bottom into
                                                view, instead of only
                                                scrolling inside the chat
                                                panel. Setting scrollTop is
                                                scoped to that one div and
                                                never touches window scroll.
  QRCheckInPage.jsx        /checkin            Vimal's phone-frame staff
                                                check-in app — four tabs:
                                                Trips (live per-coach mobile
                                                dashboard), Scan (Face/QR/
                                                Manual pills), Issues
                                                (exception logging), Me
                                                (consent lifecycle + per-
                                                delegate check-in history).
                                                Briefly DELETED (2026-07-20)
                                                as part of "Mobile UI
                                                Consolidation" — its Trips and
                                                Me tabs were pure duplicates of
                                                the real desktop Trips board
                                                and /settings + /mobile/
                                                profile, its Scan tab is fully
                                                covered by UnifiedScannerPage.
                                                jsx below (which also has
                                                Manual now, see that entry),
                                                and its Issues tab moved to
                                                the mobile Issues page (see
                                                MobileIssuesPage.jsx below) —
                                                then RESTORED the same day at
                                                the user's explicit request:
                                                "have a backup of the checkin
                                                page and put a permission to
                                                hide the page." Restored via
                                                `git show HEAD:...` (the file
                                                was never actually committed
                                                as deleted, so the last real
                                                commit WAS the backup — no
                                                second copy kept under src/,
                                                which would just be dead code
                                                again). The /checkin route,
                                                its Sidebar nav item, and the
                                                viewCheckin permission are all
                                                back exactly as they were —
                                                an admin can now hide the page
                                                per account (uncheck "Check-
                                                in" under Desktop web views in
                                                Account control) instead of it
                                                being unconditionally deleted.
  UnifiedScannerPage.jsx    /scanner            NEW (2026-07-20) — "Face Scan
                                                + QR Code Scan", a standalone
                                                desktop-native entrance-kiosk
                                                page. Re-mounts the same real
                                                check-in paths that used to
                                                live in the now-deleted
                                                QRCheckInPage.jsx (flip to
                                                ARRIVED on match) but strips
                                                everything that isn't a
                                                scanner input: no Trips/
                                                Issues/Me tabs, no low-light
                                                voice fallback, no "simulate
                                                slow" demo toggle, no
                                                biometric enrollment step.
                                                Three modes now (Manual added
                                                2026-07-20 — see below): QR
                                                slot is Jayden's
                                                QRScannerPanel.jsx completely
                                                unmodified; Face slot is a
                                                trimmed LOCAL COPY of Vimal's
                                                zero-image face vectorizer +
                                                POST /attendance/scan call
                                                (kept local since QRCheckInPage
                                                never exported its internal
                                                helpers, and this page has no
                                                dependency on that mobile-app
                                                state machine); Manual slot is
                                                Jayden's ManualTrackingPanel.
                                                jsx, also unmodified. Two-
                                                column desktop layout: big
                                                16:10 camera/panel viewport +
                                                Face/QR/Manual toggle + result
                                                card on the left, coach picker
                                                + boarded/missing stat tiles +
                                                still-missing list on the
                                                right. Ungated route (App.jsx)
                                                and Sidebar nav item — Staff
                                                and Admin roles alike can use
                                                it. Verified live as a genuine
                                                Staff-role test account
                                                (created + deleted via the
                                                real Account control API):
                                                confirmed sidebar/route
                                                access, then ran a real check-
                                                in through the QR manual-entry
                                                path end-to-end (delegate
                                                genuinely flipped to ARRIVED,
                                                reverted after).
                                                Manual mode added (2026-07-20):
                                                a third scanMode alongside
                                                Face/QR mounts Manual
                                                TrackingPanel.jsx in the same
                                                viewport (key={resetTick} like
                                                QRScannerPanel, so Reset also
                                                clears its search box) — the
                                                fallback for when a face/QR
                                                scan won't register, on the
                                                same page instead of a
                                                separate screen. Verified live:
                                                Manual mode renders the real
                                                Coach 7 roster, a real manual
                                                check-in was performed
                                                (delegate flipped to Present,
                                                coach counts updated live),
                                                reverted after via direct API.
                                                Reset button fix (2026-07-20):
                                                the top-right button used to
                                                only refetch coach data — a
                                                stuck/errored camera needed a
                                                hard reload to recover. Now a
                                                `resetTick` counter is in the
                                                Face camera effect's deps
                                                (bumping it restarts
                                                getUserMedia) AND is passed as
                                                QRScannerPanel's `key`, so a
                                                reset fully REMOUNTS the QR
                                                scanner from scratch — same
                                                guarantee a hard reload gives,
                                                without leaving the page.
                                                Renamed "Refresh" → "Reset
                                                scanner" since that's what it
                                                now does. Verified live: typed
                                                text into the QR manual-entry
                                                field, clicked reset, confirmed
                                                via DOM inspection the field
                                                came back genuinely empty
                                                (proof of a real remount, not
                                                just a data refetch).
  QRScannerPanel.jsx       (Scan → QR pill)    Jayden — live camera QR
                                                scanner (jsQR,
                                                inversionAttempts:
                                                "attemptBoth" so codes
                                                scanned off another screen
                                                still decode reliably), with
                                                a manual paste-the-code
                                                fallback when the camera is
                                                unavailable. Opens its OWN
                                                camera stream, stops every
                                                track on unmount — never
                                                contends with Vimal's face
                                                camera since the two modes
                                                are mutually exclusive.
                                                Originally expected its own
                                                JSON delegate-badge shape and
                                                posted to Jayden's own
                                                POST /api/checkins/qr — but
                                                nothing in the app printed
                                                that JSON shape (the only
                                                real badge source is Vance's
                                                "Boarding passes" tab, which
                                                encodes the plain qr_code
                                                string), so scanning a real
                                                badge always failed with "QR
                                                code invalid". Fixed by
                                                switching this panel to read
                                                the plain qr_code string and
                                                post to Vance's existing
                                                POST /api/onboarding/checkin
                                                via claudeParse.js's
                                                qrCheckin() — the same endpoint
                                                the (since-deleted)
                                                ScanToBoardView.jsx used, so
                                                there's one badge format, one
                                                resolution path. See
                                                QR_BADGE_MISMATCH.md for the
                                                history. Jayden's own
                                                checkInByQR()/POST /api/
                                                checkins/qr (exceptions.js)
                                                are now unused dead code —
                                                left in place, not deleted,
                                                since nothing else calls them
                                                and removing backend routes
                                                wasn't asked for. The old
                                                qr-test-codes/ JSON images no
                                                longer scan successfully;
                                                print a real badge from
                                                "Boarding passes" instead.
                                                NOTE: Jayden's own upstream
                                                branch later re-reversed this
                                                — it reintroduces the JSON
                                                DELEGATE_BADGE format as a
                                                second accepted shape
                                                alongside Vance's plain code,
                                                reactivating checkInByQR()/
                                                POST /api/checkins/qr.
                                                Deliberately NOT merged: that
                                                JSON format is still printed
                                                nowhere in the live app (only
                                                a demo qr-payloads.txt with a
                                                hardcoded fake delegate "d-1")
                                                — merging it would reintroduce
                                                the exact two-format ambiguity
                                                this fix eliminated, for no
                                                real functional gain. Flagged
                                                to the user and skipped by
                                                explicit choice; see the
                                                "INTEGRATING TEAMMATE
                                                BRANCHES" note near the bottom
                                                of this file.
  ManualTrackingPanel.jsx  (Scan → Manual, or   Jayden — mark a delegate
                        UnifiedScannerPage's    present by hand when a scan
                        Manual mode)            isn't possible. Reads the
                                                roster from the `coach` prop
                                                the shell already loaded (no
                                                re-fetch), search box, "Mark
                                                present" per row. Calls
                                                exceptionsApi.js's
                                                manualOverride() → POST /api/
                                                checkins/manual, gated on
                                                manageExceptions (read-only
                                                notice otherwise). Its local
                                                statusOf()/isPresent() check
                                                originally only matched the
                                                literal "PRESENT" — once the
                                                2026-07-18 migration made the
                                                backend write "ARRIVED", this
                                                would've shown already-
                                                checked-in delegates with the
                                                "Mark present" button again;
                                                fixed to accept both values.
                                                Status display (2026-07-21):
                                                each row's subtitle now shows
                                                the delegate's REAL status
                                                (Missing/Late/Assigned/Arrived/
                                                Unassigned via STATUS_LABEL,
                                                colour-matched to the Dashboard
                                                badge palette via STATUS_TONE),
                                                with any lastSeen note appended
                                                — replaces the old "No status"
                                                placeholder that ignored
                                                d.status. Sort widened to
                                                missing→late→assigned→arrived
                                                so people still needing action
                                                stay at the top.
                                                Undo (2026-07-20): an "Undo"
                                                button appears next to a
                                                delegate's "In" badge, but
                                                ONLY for one this panel's own
                                                `justMarked` Set flipped THIS
                                                session (the accidental-click
                                                case) — not for anyone
                                                already ARRIVED from an
                                                earlier scan/session, which
                                                has nothing to "undo". Calls
                                                the new undoManualOverride()
                                                → POST /api/checkins/manual/
                                                undo (same manageExceptions
                                                gate), which sets the
                                                delegate back to ASSIGNED
                                                (not MISSING — the spec's
                                                explicit target status) and
                                                deletes the single most-
                                                recent MANUAL check_in_logs
                                                row for that delegate (bounded
                                                to one row so it can never
                                                wipe an earlier real check-
                                                in). Mounted a second place
                                                now too: UnifiedScannerPage.
                                                jsx's third "Manual" scanMode
                                                (2026-07-20), unmodified,
                                                key={resetTick} so a Reset
                                                also clears its search box.
  IssuesPanel.jsx          (Me → Issues tab)   Jayden — mobile exception
                                                logging: issue-type tiles
                                                (Missing person / Lost badge
                                                / Dead phone / VIP request /
                                                Others), delegate picker,
                                                note, "Mark as critical" +
                                                a Normal/Low priority button
                                                pair underneath (superseded
                                                while Critical is on), plus
                                                the open-ticket list for this
                                                trip. "Others" reveals a
                                                free-text label input capped
                                                at TYPE_OTHER_MAX (20 chars).
                                                Same data layer as the
                                                desktop ExceptionInboxPage.jsx
                                                (exceptionsApi.js), including
                                                the live SSE "● Live"
                                                indicator. Logging/resolving
                                                needs manageExceptions. No
                                                i18n on this file (mobile
                                                Check-in shell is English-
                                                only) — unchanged by this.
  AccountControlPage.jsx   /accounts           Create/edit/delete staff
                                                accounts + permissions
                                                (needs manageAccounts).
                                                • Role selector (Staff/Admin,
                                                  no more "Main" tier) in the
                                                  create/edit form — role is a
                                                  real, independently-submitted
                                                  field (see the RBAC note
                                                  under AUTH & SECURITY above),
                                                  not derived from the
                                                  checkboxes. Picking Admin
                                                  hides the checkboxes (moot —
                                                  admin bypasses every check);
                                                  picking Staff shows the real,
                                                  enforced checkboxes.
                                                • Table now has a dedicated
                                                  Role column (badge: red for
                                                  Admin, neutral for Staff),
                                                  plus Search (username/name
                                                  substring), a role Filter
                                                  (All/Admin/Staff), and a Sort
                                                  select (Username/Name/Role) —
                                                  same UI conventions as the
                                                  Dashboard's "All delegates"
                                                  search/filter/sort bar.
                                                • Multi-select checkboxes +
                                                  "Delete selected (N)" button
                                                  (only rendered once 1+ rows
                                                  are checked) — batch-deletes
                                                  by looping the existing
                                                  single DELETE /api/accounts/
                                                  :id endpoint via
                                                  Promise.allSettled (no new
                                                  backend route). Your OWN
                                                  account's checkbox is always
                                                  disabled and excluded from
                                                  "select all," so a batch
                                                  delete can't invalidate your
                                                  own session mid-loop. If one
                                                  deletion in the batch fails
                                                  (e.g. the "last admin" guard
                                                  blocks it), the first
                                                  failure's message is shown
                                                  and the rest still complete.
                                                • A "Delete all" button sits
                                                  next to "Delete selected"
                                                  (shown whenever there's at
                                                  least one OTHER account) —
                                                  deleteAllAccounts() targets
                                                  every account except your
                                                  own (same self-exclusion
                                                  rule as the checkboxes,
                                                  same Promise.allSettled loop
                                                  over the single-delete
                                                  route) rather than literally
                                                  everyone, so you can't
                                                  accidentally lock yourself
                                                  out of Account control.
                                                • Per-page RBAC (2026-07-20):
                                                  the New/Edit modal's
                                                  checkboxes now render as 3
                                                  labelled sections (PERM_
                                                  GROUPS: "Feature actions" /
                                                  "Desktop web views" /
                                                  "Mobile views") instead of
                                                  one flat list, grouped by
                                                  each PERMISSIONS entry's new
                                                  `group` field. ROLE_PRESETS
                                                  is now derived from
                                                  DEFAULT_PERMISSIONS/
                                                  PERMISSIONS instead of a
                                                  hand-maintained duplicate
                                                  object, so a future new
                                                  permission is automatically
                                                  picked up here too. The
                                                  table's "Access" chip column
                                                  deliberately still shows
                                                  ONLY action-group
                                                  permissions (not all 16
                                                  keys) — view permissions
                                                  default open for everyone,
                                                  so listing them all would
                                                  put 8+ near-identical chips
                                                  on almost every row; the
                                                  modal is where per-page
                                                  access actually gets
                                                  configured/audited.
                                                • Collapsible sections +
                                                  select-all (2026-07-20):
                                                  each PERM_GROUPS header is a
                                                  <button aria-expanded>
                                                  with a Chevron; a
                                                  collapsedGroups Set (reset
                                                  to all-expanded every time
                                                  the modal opens fresh)
                                                  hides that section's
                                                  checkbox list entirely, not
                                                  just visually. The
                                                  desktopView/mobileView
                                                  groups only (not action —
                                                  5 checkboxes doesn't need
                                                  one, and they're rarely
                                                  all-or-nothing) get a
                                                  "Select all"/"Deselect all"
                                                  toggle button next to their
                                                  header (setAllInGroup()),
                                                  label flips based on
                                                  whether every checkbox in
                                                  THAT group is already
                                                  checked — same convention
                                                  the accounts table's own
                                                  header checkbox uses.
  SettingsPage.jsx         /settings           Signed-in account info
                                                (avatar/name/role),
                                                permissions list, and the
                                                Theme + Language preference
                                                controls. Reached via the
                                                Sidebar's account block.
                                                Renders <MediaManager /> at
                                                the bottom when
                                                perms.manageAccounts
                                                (2026-07-19) — see that
                                                component below.
  MediaManager.jsx         (in Settings)       New (2026-07-19). Thumbnail
                                                grid over GET /api/media/
                                                images (routes/media.js) —
                                                click a thumbnail to select,
                                                "Select all"/"Delete selected
                                                (N)" (window.confirm, extra
                                                warning line if any selection
                                                is a delegate's live photo),
                                                "Purge all" opens its own
                                                modal requiring the exact
                                                phrase "DELETE ALL" typed
                                                before the confirm button
                                                enables — the one genuinely
                                                destructive, no-undo action
                                                anywhere in this app. Each
                                                thumbnail is labelled with
                                                its owning delegate's name,
                                                or "Unlinked" if orphaned.
  HistoryLogPage.jsx       /history            Standalone audit-log page —
                                                the same activity_log data as
                                                the Dashboard's inline
                                                "History tracker" card (GET
                                                /api/activity), but expanded
                                                with chronological DATE
                                                stamping: entries grouped into
                                                one card per calendar day
                                                (newest first, "Today"/
                                                "Yesterday"/full weekday-date
                                                labels), mirroring the
                                                per-coach card layout of the
                                                Reverse headcount view. Each
                                                entry keeps its exact time +
                                                actor; per-entry delete +
                                                "Clear all" gated on
                                                manageDelegates (DELETE
                                                /api/activity/:id and
                                                /api/activity). Auto-refreshes
                                                every 2s (loadingRef-guarded).
                                                Reached from a "View full log"
                                                button in the Dashboard's
                                                History tracker card header
                                                (not a Sidebar item — same
                                                button-launch pattern as
                                                Reverse headcount).
  UserGuidePage.jsx        /guide              New (2026-07-19). Onboarding
                                                page for anyone new to the
                                                app — no permission gate
                                                (unlike most Sidebar items,
                                                deliberately open to every
                                                signed-in account regardless
                                                of what they can edit). A
                                                video-placeholder card (play
                                                icon, "coming soon" — no
                                                player wired up, genuinely
                                                just a placeholder per the
                                                request) followed by a plain-
                                                language explainer for all 5
                                                delegate statuses: a small
                                                flow diagram (Unassigned →
                                                Assigned → Arrived, with Late
                                                and Missing called out as
                                                separate branches) plus one
                                                card per status covering what
                                                it means, an "● Automatic" /
                                                "● Manual" tag (Late flips on
                                                its own past a trip's cutoff;
                                                Missing is ALWAYS set by hand
                                                — this is the one distinction
                                                a newcomer is most likely to
                                                miss), and a concrete "what to
                                                do" line. A "Quick tips"
                                                section covers QR-scan-is-
                                                fastest, Missing-always-needs-
                                                a-location, and cutoffs-are-
                                                per-trip. Status badge colors
                                                use the same `badge-*` classes
                                                as everywhere else in the app,
                                                so this page's swatches always
                                                match what a newcomer actually
                                                sees elsewhere — including
                                                Unassigned, which is grey
                                                app-wide as of 2026-07-19 (see
                                                tokens.css; was amber when
                                                this page was first written,
                                                deliberately kept distinct
                                                from DelegateAvatar.jsx's
                                                separate grey convention at
                                                the time — both are the same
                                                grey now). Reached on desktop
                                                from a "?" (HelpCircle) icon
                                                button in the Sidebar FOOTER,
                                                next to the theme toggle
                                                (2026-07-19 — moved out of the
                                                primary nav items, since it's
                                                a help/reference resource, not
                                                a workflow destination; sits
                                                with the utility controls
                                                instead), and on mobile from
                                                MobileProfilePage.jsx (links
                                                out to this same shared page —
                                                mobile has no dedicated
                                                onboarding surface of its own).

Mobile pages (src/pages/mobile/):

  MobileLayout.jsx          (wraps /mobile/*)   Bottom tab-bar shell, incl.
                                                 the EN/中文 toggle and (now)
                                                 a sun/moon useTheme() toggle
                                                 beside it in the topbar.
                                                 Runs useSessionGuard() (see
                                                 lib/useSessionGuard.js) —
                                                 previously only the desktop
                                                 Layout did, so a mobile
                                                 session never noticed being
                                                 invalidated (e.g. by a newer
                                                 login elsewhere) and just
                                                 kept failing every API call
                                                 instead of cleanly logging
                                                 out.
  MobileHomePage.jsx        /mobile             Rebuilt to mirror the layout
                                                 of Vimal's Check-in screen's
                                                 own "Trips" home tab
                                                 (QRCheckInPage.jsx's HomeView)
                                                 so both mobile surfaces feel
                                                 like the same app:
                                                 • Personalized greeting header
                                                   — "Good morning/afternoon/
                                                   evening" (by local hour) +
                                                   the signed-in staff name +
                                                   an "● Online · synced"
                                                   badge.
                                                 • Active trip panel — red
                                                   card, trip name, "Day X of
                                                   Y · HH:MM local", departsIn
                                                   badge. Same visual language
                                                   as the Check-in screen's own
                                                   trip card.
                                                 • Glanceable metrics —
                                                   Missing / Present / Total
                                                   (3 cards, not the previous
                                                   4 incl. Unassigned —
                                                   Unassigned stays reachable
                                                   via the Attendance page's
                                                   filter chips). Each tile is
                                                   still a shortcut into
                                                   MobileAttendancePage,
                                                   pre-filtered to that status.
                                                 • Coach status — originally a
                                                   single whole-card tap target
                                                   (always to the generic all-
                                                   coaches MISSING view); user
                                                   feedback ("don't link the
                                                   coach status when i click")
                                                   led to splitting it: the
                                                   header ("Coach status" +
                                                   chevron) is its own button
                                                   to the all-coaches MISSING
                                                   list, and each per-coach row
                                                   is now its OWN button,
                                                   linking to THAT coach's
                                                   missing list specifically
                                                   (?status=MISSING&coach=<id>
                                                   — MobileAttendancePage.jsx
                                                   reads the coach param and
                                                   shows a removable "Filtered
                                                   to <coach>" chip). Couldn't
                                                   nest a <button> inside the
                                                   outer <button> anymore once
                                                   rows became individually
                                                   clickable, so the outer
                                                   wrapper changed from
                                                   <button className="mobile-
                                                   card"> to a plain <div> with
                                                   two separate inner buttons.
                                                 Auto-refreshes every 2s (same
                                                 pattern/loadingRef guard as
                                                 the desktop Dashboard).
                                                 Issues card ("Mobile UI
                                                 Consolidation" Option A,
                                                 2026-07-20): sits right under
                                                 the KPI row and above Coach
                                                 status — icon + "Report or
                                                 view exceptions" + an open-
                                                 critical-count badge
                                                 (getCriticalOpenCount(),
                                                 polled every 2s, same
                                                 endpoint the desktop
                                                 Sidebar's own badge uses).
                                                 Was briefly an inline
                                                 expandable accordion mounting
                                                 IssuesPanel.jsx right here;
                                                 changed the SAME day
                                                 ("Mobile UI State Sync,
                                                 Revert Actions, & Navigation
                                                 Fixes" part 3) to a plain
                                                 navigate("/mobile/issues") —
                                                 see MobileIssuesPage.jsx
                                                 below — since the full form +
                                                 ticket list reads better as
                                                 its own screen than squeezed
                                                 into an accordion on a small
                                                 viewport. Verified live: tap
                                                 does a real client-side route
                                                 change (not an expand), lands
                                                 on the dedicated page.
  MobileIssuesPage.jsx      /mobile/issues      NEW (2026-07-20) — dedicated
                                                 Issues/Exceptions screen:
                                                 a back-arrow header + Jayden's
                                                 IssuesPanel.jsx, unmodified.
                                                 IssuesPanel wants a `coach`
                                                 prop shaped `{ delegates:
                                                 [...] }` keyed `delegateId`
                                                 (the shape GET /attendance/
                                                 :tripId/coach/:coachId
                                                 returns), but this page has
                                                 no single "active coach" —
                                                 fetches the FULL trip roster
                                                 via exceptionsApi.js's
                                                 existing getDelegates() (GET
                                                 /trips/:id/delegates, keyed
                                                 `id`) on mount and remaps
                                                 `id`→`delegateId`, so the
                                                 picker covers every delegate
                                                 on the trip, not just one
                                                 coach's — arguably more
                                                 useful here anyway, since a
                                                 staff member raising a ticket
                                                 on the ground may not know
                                                 which coach the delegate
                                                 belongs to. Ungated route
                                                 (same as the Home card that
                                                 links here) — no new
                                                 permission introduced.
                                                 Verified live: real
                                                 navigation from Home's Issues
                                                 card, real form + a real 39-
                                                 delegate picker + the real
                                                 open-tickets list with
                                                 working Resolve buttons and
                                                 an "● Live" SSE badge, back
                                                 arrow returns cleanly to
                                                 Home. Zero console errors.
  MobileScannerPage.jsx     /mobile/scanner     NEW (2026-07-21) — the mobile
                                                 port of the desktop /scanner
                                                 (UnifiedScannerPage.jsx),
                                                 single-column with a portrait
                                                 3:4 viewport. Same three real
                                                 check-in modes (Face / QR /
                                                 Manual) writing to the same
                                                 endpoints; shares the face
                                                 vectorizer/validator/tone with
                                                 the desktop page via lib/
                                                 faceScan.js (one copy, not
                                                 three) and mounts Jayden's
                                                 QRScannerPanel/ManualTracking
                                                 Panel unmodified. Face camera
                                                 requests the environment
                                                 (rear) camera — a handheld
                                                 phone points its back at the
                                                 delegate (desktop uses the
                                                 user-facing webcam). Auth-only
                                                 route (NOT view-gated — any
                                                 staff/admin can scan, matching
                                                 the original ungated check-in
                                                 philosophy). Reached from
                                                 WITHIN the logged-in mobile
                                                 app (not linked from the
                                                 bottom-tab nav — kept at 4
                                                 tabs to preserve the nav-
                                                 consistency fix, see the
                                                 mobile.css .mobile-shell
                                                 note). NOT the Login page's
                                                 "Quick Scanner Access" target
                                                 (that changed 2026-07-21 to
                                                 the passwordless
                                                 KioskScannerPage.jsx below —
                                                 this route still needs a real
                                                 login). Verified live at
                                                 375×812: real Manual check-in
                                                 + Undo worked through it,
                                                 zero console errors.
  KioskScannerPage.jsx      /kiosk-scan         NEW (2026-07-21) — the
                                                 PASSWORDLESS entrance-kiosk
                                                 scanner (Face + QR only, no
                                                 Manual — that stays a staff-
                                                 only override behind a real
                                                 login). Registered in App.jsx
                                                 OUTSIDE both Layout and
                                                 MobileLayout, and outside the
                                                 `authed`-gated Routes tree
                                                 entirely (the one route
                                                 present in BOTH the logged-
                                                 out and logged-in <Routes>),
                                                 so it renders with ZERO nav
                                                 chrome — no sidebar, no tab
                                                 bar. The only way out is its
                                                 own "Back to Login" button,
                                                 which stops the camera stream
                                                 and navigates to /login. On
                                                 mount it POSTs /api/auth/
                                                 kiosk (no credentials) and
                                                 holds the returned token in a
                                                 useRef — deliberately NEVER
                                                 localStorage/sessionStorage
                                                 and never touches lib/api.js's
                                                 getToken(), so visiting this
                                                 route can't affect or leak
                                                 into a real logged-in session
                                                 on the same device. Reuses
                                                 lib/faceScan.js for the face
                                                 vectorizer/validator/tone;
                                                 QR-decodes with jsqr directly
                                                 in a small self-contained loop
                                                 (mirrors QRScannerPanel.jsx's
                                                 pattern rather than importing
                                                 it, since this page's camera/
                                                 token/network plumbing is all
                                                 its own — it can't use the
                                                 shared lib/api.js fetch
                                                 wrapper, which assumes a real
                                                 session token). Reached from
                                                 the Login page's "Quick
                                                 Scanner Access" button — a
                                                 PLAIN client-side navigate(),
                                                 no form validation, no API
                                                 call. Security model: the
                                                 page itself needs no auth,
                                                 but the token it mints is
                                                 narrowly scoped server-side
                                                 (see server.js's POST /api/
                                                 auth/kiosk entry above) to
                                                 grant ONLY the two camera
                                                 check-in endpoints — so it's
                                                 reachable with no password,
                                                 yet can't read or touch
                                                 anything else in the app.
  MobileAttendancePage.jsx  /mobile/attendance  Full delegate roster — search
                                                 + status filter chips (reads
                                                 an initial ?status= from the
                                                 URL, so Home's KPI tiles AND
                                                 its Coach status card both
                                                 deep-link straight into a
                                                 filtered view).
                                                 Row layout redesigned to match
                                                 the "card row between" pattern
                                                 already used by Vimal's Manual
                                                 check-in (ManualTrackingPanel.
                                                 jsx) and "Me" tab (QRCheckIn
                                                 Page.jsx's MeView) — avatar +
                                                 name/subtitle on the left, a
                                                 clear action button on the
                                                 right, one delegate per
                                                 `.mobile-card`. Status is no
                                                 longer edited via a cramped
                                                 inline <select> — an explicit
                                                 "Update status" button (gated
                                                 on manageDelegates; read-only
                                                 StatusBadge-style pill
                                                 otherwise) opens a bottom-
                                                 sheet (StatusSheet, local to
                                                 this file) listing all 5
                                                 statuses (Unassigned/Assigned/
                                                 Arrived/Late/Missing, was just
                                                 3) as big obvious buttons —
                                                 same "one clear action per
                                                 row" spirit as Manual check-
                                                 in's own "Mark present"
                                                 button. Optimistic update,
                                                 rolls back + shows an inline
                                                 error on failure.
                                                 5-status extension: FILTERS/
                                                 STATUS_OPTIONS/FILTER_LABEL
                                                 all expanded to the 5 values;
                                                 a local STATUS_BADGE_CLASS map
                                                 + effectiveStatus() helper
                                                 (aliases legacy "PRESENT" rows
                                                 to "ARRIVED", same pattern as
                                                 Dashboard/TripCoachPage) fixed
                                                 a real bug this surfaced —
                                                 before this, an ARRIVED row
                                                 fell through the old 3-way
                                                 ternary straight to the
                                                 UNASSIGNED badge class/color,
                                                 rendering as an amber
                                                 "ARRIVED" pill instead of the
                                                 correct green one. Also gained
                                                 a coachFilter (?coach=<id>,
                                                 see the Coach status card note
                                                 above). Originally just a
                                                 removable "Filtered to
                                                 <coach>" indicator chip (only
                                                 settable by arriving via that
                                                 URL param); per a follow-up
                                                 request for "a robust dropdown
                                                 ... to filter by Coach" on
                                                 this page directly, replaced
                                                 with an always-visible <select>
                                                 (All coaches + every real
                                                 coach) next to the search box
                                                 — coachFilter now works both
                                                 ways: incoming from Home's
                                                 per-coach link, or picked
                                                 manually here. The clear (X)
                                                 button only shows once a coach
                                                 is actually selected.
                                                 Spacing tightened complaint —
                                                 header/search/filter-row
                                                 margins and the filter chips'
                                                 own gap/padding were bumped up
                                                 (14→18, 10→14, 6→8 gap /
                                                 12→16 margin, 6px→7px chip
                                                 padding) and each delegate
                                                 card's inner row gap 10→12,
                                                 padding 14→16 — was reading as
                                                 cramped on a real phone.
                                                 Every MISSING or LATE
                                                 delegate gets a one-tap `tel:`
                                                 call button (callDelegate(),
                                                 formerly gated on `d.phone`
                                                 already being populated —
                                                 changed 2026-07-18 after that
                                                 turned out to hide the button
                                                 entirely, since none of the
                                                 demo delegates have a phone
                                                 on file; now prompts for one
                                                 via window.prompt the first
                                                 time, saves it back onto the
                                                 delegate through the existing
                                                 PATCH, then calls — and was
                                                 extended from MISSING-only to
                                                 also cover LATE, since staff
                                                 want to ring someone running
                                                 behind the cutoff too, not
                                                 just someone unaccounted for).
                                                 delegates.phone already
                                                 existed (added by Vance's
                                                 onboarding parser for the
                                                 document-upload flow), so this
                                                 needed no new backend column.
                                                 Each row also still shows a
                                                 map-pin button when that
                                                 delegate has a lastLocation
                                                 set (tinted red for MISSING),
                                                 opening the same
                                                 DelegateLocationMap component
                                                 used on desktop.
                                                 Auto-refreshes every 2s; the
                                                 poll is paused (via a
                                                 savingIdRef) while a row's
                                                 status edit is still being
                                                 saved, so a background refresh
                                                 can't land mid-save and briefly
                                                 flash the pre-edit status back
                                                 in before the optimistic
                                                 update's own rollback/confirm
                                                 logic resolves.
  MobileAssistantPage.jsx   (no longer routed — Mobile chat — same real AI
                             see below)          assistant as the desktop one
                                                 (Vance; POST /api/chat/messages).
                                                 No longer a dedicated tab/
                                                 route — now rendered
                                                 `embedded` inside
                                                 MobileChatBubble.jsx (a
                                                 floating widget on every
                                                 /mobile/* page, replacing the
                                                 old "Assistant" bottom tab).
                                                 Added an `embedded` prop
                                                 (default false) that hides
                                                 its own small header block
                                                 and fills its container's
                                                 height instead of
                                                 `calc(100vh - 180px)` — the
                                                 chat logic itself (POST
                                                 /api/chat/messages,
                                                 stateless single-
                                                 conversation) is completely
                                                 unchanged.
  MobileChatBubble.jsx      (floating widget)   New. FAB (bottom-right,
                                                 positioned ABOVE the fixed
                                                 .mobile-tabbar so it doesn't
                                                 overlap the nav) that opens a
                                                 full-screen bottom-sheet
                                                 overlay containing
                                                 `<MobileAssistantPage
                                                 embedded />`. Rendered once
                                                 from MobileLayout.jsx so it
                                                 persists across every mobile
                                                 route. No longer strictly
                                                 "bottom-right" as of
                                                 2026-07-18 — draggable to any
                                                 of the four corners, position
                                                 persisted to localStorage.
                                                 Scroll-reveal + auto-hide +
                                                 drag-to-corner (2026-07-18):
                                                 same activity-based
                                                 `recentlyActive`/5s-timer/
                                                 Pointer-Events-drag pattern
                                                 as desktop ChatBubble.jsx —
                                                 hidden by default, fades in
                                                 on scroll, fades out 5s after
                                                 scrolling stops, stays
                                                 visible once opened or while
                                                 being dragged. See
                                                 ChatBubble.jsx's entry above
                                                 for the full rationale.
  MobileTripsPage.jsx       /mobile/trips       New. Genuinely blank
                                                 placeholder (eyebrow + h1 +
                                                 "Coming soon.") — took over
                                                 the tab slot freed up by
                                                 Assistant becoming a bubble.
                                                 Desktop already has Desmond's
                                                 full Trip/Coach board
                                                 (TripCoachPage.jsx); mobile
                                                 has no equivalent yet. Left
                                                 for a teammate to build out —
                                                 not built here.
  MobileProfilePage.jsx     /mobile/profile     Account info + logout.

Styles (src/styles/):

  tokens.css
      The whole design system — colors, buttons, cards, badges, tables,
      the sidebar/page layout. Loaded globally. .select now gets a custom
      chevron-down icon (appearance:none + an inline SVG background-image)
      instead of each browser's own native arrow — any inline style on a
      <select className="select"> that overrides padding must leave room on
      the right (padding-right >= ~28px) or the icon will overlap the text;
      see OnboardingPage.jsx's coach-picker and MobileAttendancePage.jsx's
      status <select> for the pattern.
      `html { zoom: calc(var(--app-zoom) * 100%); }` (--app-zoom: 0.99 —
      originally 1.1, lowered per user preference; see below — defined in
      :root) bakes in a fixed visual scale so the app reads at a
      consistent, comfortable size at every viewer's own 100% browser zoom —
      same look across Chrome/Edge/Safari/Firefox 126+, since it's part of
      the page instead of a per-browser manual zoom setting. Uses `zoom` (not
      `transform: scale`) so it participates in normal layout — scrollbars,
      viewport units, and click hit-testing all stay correct in MOST cases
      (see the --sidebar-w / .sidebar note below for the one real exception
      this surfaced).
      Dark theme (:root[data-theme="dark"]) was softened — was near-black
      (--bg #121417, --surface #1a1d22); now a lighter charcoal (--bg
      #1a1d21, --surface #24272c, --surface-2 #2a2e33), same relative
      structure (bg < surface < surface-2), just lifted several steps.
      .sidebar's `height: 100vh` — a REAL bug, not just cosmetic, was found
      here: `zoom` scales the RENDERED box of everything, but `vh` units and
      the browser's own overflow detection (scrollHeight/clientHeight) are
      computed in PRE-zoom layout space. So on a viewport-CLAMPED sticky
      element like the sidebar, `height: 100vh` rendered as 100vh × 1.1 of
      ACTUAL visual space — the bottom ~10% (the Log out button) silently
      overflowed the real screen, and because the browser's own layout math
      didn't consider it "overflowing" (both measured pre-zoom, so they were
      equal), `overflow-y: auto` didn't even trigger a scrollbar to reach it.
      A real user got completely locked out of the Log out button. Two
      wrong attempts before the real fix: (1) `overflow-y: hidden` traded
      the "unwanted scrollbar" complaint for silently clipping the footer
      with zero way to reach it — worse, not better; (2) just restoring
      `overflow-y: auto` alone didn't help either, since (per the paragraph
      above) the browser doesn't detect this as overflow in the first place.
      The actual fix: `.sidebar`'s height is `calc(100vh / var(--app-zoom))`
      — dividing cancels the zoom's scale-up so the RENDERED box is exactly
      one real viewport tall, matching what `100vh` is supposed to mean.
      `overflow-y: auto` is kept as a defensive safety net on top (content
      that still somehow doesn't fit — an unusual OS font size, a longer nav
      list later — stays reachable by scrolling instead of disappearing).
      Sidebar padding/gaps were also trimmed a little (20px→16px container
      padding, 4px→2px item gap, 10px→8px per-item vertical padding) so
      scrolling is rarely actually needed on a normal screen. Any OTHER
      element that uses `100vh`/`100vw` for a viewport-CLAMPED (not
      page-scrollable) size must divide by `--app-zoom` the same way — plain
      page content using `min-height: 100vh` (like `.app-shell`) is fine
      as-is, since overshooting there just makes an already-scrollable page
      a little taller, which is harmless.
      --app-zoom later changed from 1.1 to 0.99: user compared the app in
      Chrome at their browser's default 100% zoom (effective size = 1.0 ×
      1.1 = 110%) against manually zooming Chrome to 90% (effective size =
      0.9 × 1.1 = 99%) and preferred the smaller one. Rather than ask every
      viewer to manually zoom their browser, baked that same ~99% net size
      in as --app-zoom directly, so it's the default at anyone's normal
      100% browser zoom. The `calc(100vh / var(--app-zoom))` sidebar-height
      fix above is a general formula that holds at any zoom value above OR
      below 100% — no additional change needed there when this was lowered.

  mobile.css
      Mobile-only classes (.mobile-shell, .mobile-card, .mobile-tabbar,
      etc.), scoped so it can't clash with the desktop styles.
      .mobile-shell owns the scroll (2026-07-21 fix): `height:100%;
      overflow-y:auto; scrollbar-gutter:stable`. WHY — the fixed bottom
      .mobile-tabbar is positioned against the viewport, so when the document
      BODY gained a scrollbar on tall pages (the Attendance roster) but not
      on short ones (Home), the bar's right edge moved by the scrollbar width
      and its evenly-spaced tabs visibly shifted between pages. Making the
      shell the scroll container means the body never scrolls (constant
      viewport width) and the reserved gutter keeps content width identical
      on every mobile view — so the tab bar is now pixel-static across Home /
      Attendance / Trips / Profile / Issues / Scanner. Scoped to .mobile-
      shell on purpose: mobile.css is imported app-wide via App.jsx's static
      MobileLayout import, so a global html/body rule here would leak to
      desktop.


HOW TO TRACE A PAGE
--------------------
1. Start at App.jsx to find which page a URL loads.
2. Open that page file.
3. If it needs live data, it calls apiGet/apiPost from lib/api.js.
4. That hits a route in backend/server.js (or backend/routes/*.js).
5. Which calls a function in backend/data.js (or, for AI Insights,
   talks to local Ollama / the Anthropic API).


AUTH & SECURITY
-----------------
- Session tokens are signed JWTs (backend/auth.js), not forgeable.
  Set JWT_SECRET in backend/.env before deploying anywhere public.
- Passwords are bcrypt-hashed (backend/data.js). Legacy plaintext rows
  auto-upgrade on next successful login — nobody needs to run a migration.
- Every API route requires a signed-in account at minimum; mutating
  actions (delegate CRUD, accounts, export) additionally require the
  matching permission from permissions.js, enforced server-side (not
  just hidden in the UI).
- Because tokens changed format, everyone gets signed out once after
  this update and needs to log back in — expected, one-time.

- RBAC — EXACTLY TWO ROLES (Admin / Staff). The old "main" tier (derived
  whenever manageAccounts was ticked) is retired; role is now an independent,
  explicitly-set field (the Account control role picker), not derived from
  permission checkboxes.
    · Admin BYPASSES every permission check. accountPermissions(row) in
      data.js returns every key as `true` for role="admin" regardless of
      what's actually stored — so requirePermission() (auth.js) and every
      getPermissions().xyz check on the frontend automatically unlock for
      admins, with zero per-call special-casing. Admin sees every Dashboard
      tab (Delegate/Analytics/Staff operations), every Sidebar item, and
      Account control.
    · Staff are governed by the stored checkboxes (permissions.js's 5 keys),
      defaulting to just "Manage delegates" — i.e. the Dashboard's Delegate
      tab, the typical staff-facing surface. DashboardPage.jsx additionally
      hard-gates the Analytics tab on role==="admin" (not a checkbox) — a
      Staff account never sees it, however the permissions are configured.
    · "Last admin" protection: updateAccount()/deleteAccount() in data.js
      block demoting or deleting the only account with role="admin" (would
      lock everyone out of Account control) — countAdmins() replaces the old
      countManagers()/manageAccounts-flag check. Error code stays "LAST_MAIN"
      for frontend compatibility; the displayed message now says "Admin".
    · Migration: seed()'s startup routine runs
      `UPDATE accounts SET role = 'admin' WHERE role = 'main'` every boot
      (idempotent — a no-op once none are left), so the team's shared Neon
      DB picks this up automatically on next deploy/restart, no manual DB
      edit needed. backend/seed-team.js and backend/reset-login.js were also
      updated to seed role='admin' instead of 'main'.
    · AccountControlPage.jsx's role picker always shows a Staff/Admin toggle
      (no more "this is the protected Main account, checkboxes only"
      special case) — picking Admin hides the permission checkboxes
      entirely (they're moot; admin bypasses regardless) and shows a note
      instead; picking Staff shows the real, enforced checkboxes.
- "Remember password" (login page) is now SECURE / TOKEN-BASED — it no
  longer stores the password anywhere. Ticking it only controls whether the
  signed JWT persists in localStorage (survives closing the browser) vs
  sessionStorage (cleared when the tab/browser closes), via the existing
  setToken(token, keep) call. LoginPage.jsx purges any plaintext password an
  earlier build already wrote under the old "mg_remember"/"mg_remember_v2"
  keys on load, for every returning visitor. This reverses the earlier
  restored plaintext-password-remembering behavior (see AI Log) at explicit
  user request, for a different, security-focused reason this time.


TESTING THE MOBILE UI ON A PHONE
-----------------------------------
Both dev servers already support this (vite.config.js has host: true,
Express listens on all interfaces by default) — no code changes needed:

  1. Connect your phone to the same Wi-Fi as this computer.
  2. Run `npm run dev` in both backend/ and frontend/ as usual.
  3. Find this computer's local IP (Windows: ipconfig -> IPv4 Address,
     e.g. 192.168.1.11).
  4. On the phone's browser, go to http://<that-IP>:5173/login, sign in,
     and it'll land you on /mobile automatically.
  5. If it won't load, Windows Firewall is the usual culprit — allow
     Node.js through the firewall for Private networks (or allow ports
     5173/4000). The phone only needs port 5173 — Vite's dev proxy
     forwards /api/* to the backend on this machine's side.


AI INSIGHTS — RUNNING IT LOCALLY
----------------------------------
Two options, tried in this order automatically — no code changes needed
to switch between them:

  a) Local Ollama (free, no API key, nothing leaves your machine)
     1. Install from ollama.com
     2. Run: ollama pull llama3.2
     3. Leave it running in the background — auto-detected at
        http://localhost:11434

  b) Anthropic API (used automatically if Ollama isn't running)
     1. Get a key at console.anthropic.com
     2. Add to backend/.env:  ANTHROPIC_API_KEY=sk-ant-...

If deploying the backend to a live hosting service, only option (b)
works there — the deployed server has no access to your laptop's
Ollama. Add ANTHROPIC_API_KEY as an environment variable on the
hosting platform (Vercel/Render/etc.) for AI Insights to work live.


CAN'T LOG IN AFTER CLONING? (shared-database gotcha)
--------------------------------------------------------
The team shares ONE Neon database. `git clone` resets your CODE but NOT the
database — so the seeded `staff_194 / password123!` login (which only runs
on a first boot against an empty DB) may not match what's currently stored
in the shared DB. If someone changed the password, or a teammate on an older
pre-bcrypt branch wrote to the accounts table, the documented credentials
stop working for everyone.

Fix it in one command (no more editing Neon by hand):

  cd backend
  npm run reset:login        # staff_194 back to password123!, full access

Note: because logins now enforce a single active session, when YOU reset and
log in, other people's staff_194 sessions get logged out. Longer term, each
developer having their own Neon database (free tier allows several) avoids
this class of problem entirely.


GETTING LOGGED OUT EVERY ~15 SECONDS? ("Your account is no longer valid")
------------------------------------------------------------------------
Logins enforce a SINGLE ACTIVE SESSION per account (auth.js bumps a
token_version on every login; the 15s session poll logs out any session whose
token is behind). That is correct for one person — but if the WHOLE TEAM
shares `staff_194` on the shared database, every login anywhere (a teammate,
another clone, another tab) invalidates everyone else, so you get kicked out
constantly even while idle.

Fix: give each person their OWN account, then sign in as yourself, not
staff_194:

  cd backend
  npm run seed:team          # creates jq / vance / desmond / jayden / vimal
                             # (each "password123!", full access)

Your session then only ends when YOU log in elsewhere — not when a teammate
does.


EXCEPTIONS FEATURE — SEEDING DEMO DATA
------------------------------------------
The Exception inbox and Trips board start empty on a fresh database (the
base app only seeds a trip, 4 coaches, and the first login). To populate
them with a realistic delegate roster + 7 sample tickets:

  cd backend
  npm run seed:demo

Safe to re-run — it skips seeding whatever already exists. By default,
new accounts (and any created before this feature) don't have the
"Manage exceptions" permission — grant it in Account Control to let an
account raise/resolve/delete tickets or do a manual override; everyone
signed in can already view the inbox and live alerts.


INTEGRATING TEAMMATE BRANCHES (e.g. from C:\fsad\Project\integration\...)
---------------------------------------------------------------------------
Teammates' work sometimes arrives as a full clone/checkout of their branch in
a sibling `integration/` folder rather than a PR against this repo. Two
lessons learned merging Jayden's `SecureScan-Logs-(Jayden)` branch a second
time (2026-07-17, after an earlier successful merge on 2026-07-15):

1. CONFIRM IT'S REALLY NEWER before touching anything. `git fetch origin`
   inside the integration clone + compare against its tracked remote branch —
   don't assume a folder labeled "integration" is automatically ahead of what
   was already merged. This time it genuinely was (a real new commit,
   "QR scanner: accept Vance's boarding-pass codes" — coincidentally the same
   headline as a fix already made independently in this project days earlier,
   which is what makes checking essential rather than assuming).

2. DIFF EVERY FILE, but expect MOST diffs to be false positives. A teammate's
   branch is usually an old snapshot relative to this project's own fast-
   moving trunk — diffing shared/foundational files (data.js, server.js,
   App.jsx, Sidebar.jsx, tokens.css, i18n.jsx, lib/api.js, any page not owned
   by that teammate) will show "differs" almost everywhere, but that's THIS
   PROJECT having moved on, not the teammate having added something. Blindly
   overwriting any of those would silently REVERT real, already-shipped work
   (this pass alone would have reverted the RBAC/dark-theme/zoom/rate-
   limiting/mobile-refactor changes from the same day). Only cherry-pick the
   files the branch's OWNER actually touched for their OWN feature, and even
   then verify feature-by-feature, not file-by-file:
     - A file can carry BOTH a genuine new feature AND stale drift in the
       same diff (Jayden's exceptions.js/IssuesPanel.jsx/LogExceptionModal.jsx
       had a real, valuable "Others" issue-type + Low-priority feature mixed
       in with hardcoded `#fff` backgrounds that would have broken dark mode,
       and — in the two exception-inbox files specifically — every
       `useLang()`/`t()` call stripped entirely, which would have silently
       killed Chinese translation on that page). The fix was to hand-port
       just the new logic into the CURRENT versions of those files (Edit, not
       Read+Write-over), not take the diff wholesale.
     - A file can look like a genuine new feature but be a REGRESSION of a
       deliberate earlier fix (QRScannerPanel.jsx reintroduced the JSON
       DELEGATE_BADGE format + Jayden's own POST /api/checkins/qr endpoint
       that an earlier merge had specifically retired — see the
       QRScannerPanel.jsx entry above). Trace whether the "new" thing is
       actually reachable in the live app (grep the WHOLE branch for what
       generates/prints it, not just what accepts it) before assuming it's
       worth merging.
   When something like this shows up, STOP and ask before finalizing — don't
   silently pick a side. This time: presented both findings (the real feature
   + the two conflicts) to the user with concrete evidence, and asked how to
   proceed; the offered options were "cherry-pick the safe feature only"
   (chosen), "also take the reverted QR format," or "show me the raw diffs
   first."
